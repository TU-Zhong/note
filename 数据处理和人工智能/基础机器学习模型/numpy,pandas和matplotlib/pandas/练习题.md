好的，为了帮助您系统地掌握 Pandas，我为您精心设计了一套学习路径，包含 **10个核心知识点** 和配套的 **300道练习题**。

这个计划旨在让您从基础到高级，逐步深入 Pandas 的数据处理世界。每个知识点都配有专门的数据集（大部分可以直接通过代码生成或从`seaborn`库加载，无需下载文件）和一系列由浅入深的实战问题。

---

### **学习结构**

1. **核心知识点 (Core Concepts)**: 对每个关键领域进行简要介绍。
    
2. **准备数据集 (Dataset Setup)**: 在每个模块开始前，提供生成或加载数据集的代码。
    
3. **练习题 (Practice Questions)**: 围绕该知识点的具体操作任务，共300道。
    

---

### **准备工作：安装必要的库**

在开始之前，请确保您已经安装了 pandas 和 seaborn。

Python

```
pip install pandas numpy seaborn
```

### **模块一：Pandas 核心对象：Series 和 DataFrame (30题)**

**核心知识点**:

- `Series`: 一维带标签的数组，是构成 DataFrame 的基础。
    
- `DataFrame`: 二维带标签的数据结构，拥有行和列，是 Pandas 中最核心的数据结构。
    
- 了解如何从列表、字典、NumPy 数组等创建这两个对象。
    

准备数据集:

本模块的练习主要基于手动创建数据，无需加载外部文件。

**练习题**:

**Series 创建 (1-10)**

1. 从一个 Python 列表创建一个 Pandas Series。
    
2. 创建一个带有自定义索引（'a', 'b', 'c'）的 Series。
    使用 .index自定义 `s2.index=['A','B','C']`
3. 从一个 Python 字典创建一个 Series。
    
4. 创建一个包含5个元素的 Series，其数据为 NumPy 的随机数。
    ==d4=np.random.randint(0,10,5)     .rand(5)       .randn(5)==
5. 创建一个 Series，并为其指定一个名字 (name)。
    
6. 创建一个 Series，并检查其数据类型 (dtype)。
    
7. 创建一个 Series，其中包含缺失值 (NaN)。
    ==直接使用np.nan填充，isnull()得到布尔索引，dropna() 丢弃缺失值，fillna()填充缺失值==
8. 使用 `pd.date_range` 创建一个包含7个日期的 Series 作为索引。
    `dates_index = pd.date_range('2025-10-01', periods=7)`
    end=        ==指定结束日期==
9. 创建一个 Series，其索引和值都是从0到9的数字。
    s9=pd.Series(np.random.randint(0,9,10),index=np.arange(0,10)),
    ==arange(0,10)是生成0-9一共10个数==
10. 将一个已有的 Series 的索引更改为新的字母索引。
    

DataFrame 创建 (11-30)

11. 从一个包含列表的字典创建一个 DataFrame。

12. 从一个 NumPy 二维数组创建一个 DataFrame。
	==`np.array([[],[],[]])`==
13. 创建一个 DataFrame，并为其指定行索引和列名。

14. 从一个==包含字典的列表==创建一个 DataFrame。

15. 创建一个 5x3 的 DataFrame，其值为随机整数。

16. 创建一个 DataFrame，其中一列是字符串，一列是整数，一列是浮点数。
	==传递一个字典，键值key是列名，值是列的值==
17. 创建一个 DataFrame，并使用 df.dtypes 检查每列的数据类型。
==object是表示string的基本类型==
18. 创建一个 DataFrame，并使用 df.columns 获取其所有列名。

19. 创建一个 DataFrame，并使用 df.index 获取其索引。

20. 从一个已有的 Series 对象创建一个 DataFrame。

21. 创建一个 DataFrame，其中包含 datetime 类型的列。
==原字符串日期，加上to_datetime()==             [[数据分析/面试准备/基础机器学习模型/numpy,pandas和matplotlib/pandas/题解/T21|T21]]
22. 创建一个空的 DataFrame，并指定列名。

23. 向一个空的 DataFrame 中逐行添加数据。
[[数据分析/面试准备/基础机器学习模型/numpy,pandas和matplotlib/pandas/题解/T23|T23]]        ==可用concat(),`loc[]`直接赋值，推荐使用list将所有信息收集之后再使用pd来保存==
按行添加本质上是按照一个字典的列表逐个元素添加
24. 创建一个 DataFrame，其列名包含特殊字符（如空格）。
==1.使用中括号来使用列，2.在使用query函数的时候使用反引号将列名括起来==
25. 将一个 DataFrame 的某一列设置为其新的索引。
直接使用.index进行赋值
26. 创建一个多级索引 (MultiIndex) 的 DataFrame。
==使用`set_index([])`来创建多级索引,==
27. 从一个 CSV 格式的字符串中直接读取数据创建 DataFrame。

28. 创建一个 DataFrame，并使用 df.values 将其转换为 NumPy 数组。
==直接使用 .values即可得到一个numpy数组==
29. 创建一个 DataFrame，并使用 df.shape 查看其维度。

30. 复制一个已有的 DataFrame，并验证新旧 DataFrame 是独立的。

---

### **模块二：数据查看与基本信息统计 (25题)**

**核心知识点**:

- 如何快速预览数据、了解其维度、数据类型和基本统计特性。
    
- 常用函数: `head()`, `tail()`, `info()`, `describe()`, `shape`, `dtypes`, `value_counts()`。
    

准备数据集:

我们将使用 seaborn 库中经典的 titanic 数据集。

Python

```
import pandas as pd
import seaborn as sns

# 加载数据集
titanic = sns.load_dataset('titanic')
```

练习题:

31. 显示 titanic DataFrame 的前5行。

32. 显示 titanic DataFrame 的后3行。

33. 查看 titanic DataFrame 的基本信息（非空值、数据类型等）。

34. 获取 titanic DataFrame 的维度（行数和列数）。

35. 获取 titanic DataFrame 的所有列名。

36. 统计 titanic 数据集中数值类型列的基本描述性统计信息。
==默认是包含数值型数据列==
37. 统计 titanic 数据集中所有类型列（包括对象类型）的描述性信息。
==在describe()中加上include=all==           freq出现次数最多的那个值出现了多少次，只针对分类型数据
38. 查看 'sex' 列的唯一值及其出现次数。
	`train['Sex'].value_counts()`
39. 查看 'pclass' 列有多少个唯一值。
==nunique方法==
40. 查看 'embarked' 列的唯一值列表。
==.unique()方法==
41. 计算 titanic DataFrame 的总元素个数。
==由.size计算==
42. 检查 titanic DataFrame 中每列的缺失值数量。
==isnull().sum()==     
43. 检查 titanic DataFrame 中是否存在任何缺失值。

44. 计算 'age' 列的平均值。

45. 计算 'fare' 列的中位数。
.median()
46. 找出 'age' 列的最大值和最小值。
==f'{MaxAge} , {MinAge}'==   数值和字符串拼接，或者在里面使用==str(数值)==
47. 查看 'who' 列的众数（出现次数最多的值）。
==.mode()计算众数==
48. 计算 'fare' 列的方差和标准差。
	可以放进自己的故事，比如进行分组之后，对每一层的客户进行购买金额的方差检验，得出第一层的方差很大，其他层的没那么大
	==所有的放在一起算方差，很大，没有作用；可达几百亿；==
	==分层后可能就只有几亿了，更具有可预测性==
	
	==FareStds=train.groupby('Pclass')['Fare'].std()==
	### 总结一下，你应该怎么看：
	**先看标准差，而不是方差。** 因为标准差的单位和你的数据是一样的，更容易理解。
	 **将标准差与平均值进行比较。**
    - 如果标准差远小于平均值，说明数据很集中。
    - 如果标准差很大，甚至接近或超过平均值（就像票价这个例子），说明数据非常分散，波动性极高，很可能存在**异常值 (Outliers)**。
	**用来比较不同组的数据。** 例如，你可以分别计算一等舱和三等舱乘客票价的标准差。你会发现，一等舱票价的标准差肯定比三等舱大得多，因为一等舱票价的变化范围（从几十到几百）远大于三等舱（基本都在一个很小的范围内）。
	
45. 使用 nunique() 统计每列唯一值的数量。
==直接 df.nunique()就可以了==
46. 随机抽取 titanic DataFrame 中的3行数据进行查看。
可以直接使用len(df)来查看行数   
print(len(train.iloc[1]))   查看列数    ==需要保证一定有行数据，不推荐这么写==
==1. df.shape[1]               ,            2. len(df.columns)==
```python
train.sample(n=3)

list=np.random.randint(0,len(train),3)

train.iloc[list]
```
45. 查看 titanic DataFrame 占用的内存大小。
train.==memory_usage==(deep=True).sum()
train.==memory_usage==(deep=True)表示每一列的内存占用大小，sum()表示总和加起来，deep=True表示深层计算
46. 获取 'age' 列的第90个百分位数。
train['Age'].==quantile(0.9)==，这个值不一定会存在在原数据集里面
47. 计算 'survived' 列中 0 和 1 的比例。

48. 按 'pclass' 列的值出现的频率降序排序。
==`train['Pclass'].value_counts().sort_values(ascending=False)`==
49. 找出 titanic 数据集中重复的行数。
==train.duplicated().sum()==
要找出 titanic 数据集中重复的行数，您可以使用 `duplicated()` 和 `sum()` 这两个方法的组合。
- `df.duplicated()`: ==这个方法会检查每一行是否与它**前面出现过的某一行**完全相同。如果相同，则将该行标记为 `True`，否则为 `False`。==
- `.sum()`: 对 `duplicated()` 返回的布尔值序列进行求和。`True` 会被当作 `1`，`False` 会被当作 `0`，因此求和的结果就是被标记为重复的总行数。
---

### **模块三：数据选择与索引 (45题)**

**核心知识点**:

- 使用 `[]`, `.loc`, `.iloc` 和布尔索引进行精确的数据选择、切片和筛选。
    
- `.loc`: 基于标签 (label) 的索引。
    
- `.iloc`: 基于整数位置 (integer position) 的索引。
    

准备数据集:

继续使用 titanic 数据集。

**练习题**:

基础选择 (56-70)

56. 选择 'age' 这一列，返回一个 Series。
使用`train['Age']`或者`train.Age`来选择Age这一列，使用==.dtype==来得知==容器内部存储的列的类型==，使用==type(a)来得知a本身的类型==。
57. 同时选择 'age' 和 'fare' 这两列，返回一个 DataFrame。
直接方括号  `df[['','','']]`
58. 使用 [] 切片选择 DataFrame 的第 5 到第 10 行。

59. 使用 .iloc 选择第 1 行的所有数据。

60. 使用 .iloc 选择前 5 行的所有数据。

61. 使用 .iloc 选择第 0 行、第 0 列的单元格数据。

62. 使用 .iloc 选择第 1、3、5 行和第 0、2 列的数据。

63. 使用 .iloc 选择最后一行数据。
==`train.iloc[-1]`==
64. 使用 .loc 选择索引为 0 的行数据。
==按索引的名字查找==， 默认为整数索引
65. 将 titanic 的 'pclass' 列设为索引，然后使用 .loc 选择 pclass 为 1 的所有行。 (操作后请重置索引 titanic.reset_index(inplace=True))

66. 使用 .loc 选择索引为 3 到 7 的行。

67. 使用 .loc 选择索引为 5 的 'fare' 列的值。

68. 使用 .loc 同时选择 'age', 'sex', 'class' 这三列。
`train.loc[:,['Age','Sex','Pclass']]` ，==前面要有冒号==
69. ==使用 at 快速获取索引为 10 的 'age' 值 (比 .loc 更快)。==
`at[行标签，列标签]`      ，     `iat[整数位置，整数位置]`
70. 使用 iat 快速获取第 10 行、第 3 列位置的值 (比 .iloc 更快)。

布尔索引 (71-90)

71. 筛选出所有 'age' 大于 30 的行。

72. 筛选出所有性别为 'female' 的乘客。

73. 筛选出 'pclass' 为 1 且 'age' 小于 30 的所有乘客。
&
74. 筛选出 'fare' 小于 10 或大于 200 的乘客。
|
75. 筛选出 'embarked' 地点为 'S' 或 'C' 的乘客。

76. 使用 isin() 方法筛选出 'pclass' 为 2 或 3 的乘客。
```python
list=[2,3]
mask=train['Pclass'].isin(list)
train[mask]
```
76. ==筛选出 'age' 值不为空的所有行。==
	1. notna(),notnull(),     `train[train['Age'].notna()]`
	2. dropna(subset=[]),  
	3. ~df[].isnull()            `train[~train['Age'].isna()]`
77. 筛选出 'deck' 列值为空的所有行。

78. 筛选出 'who' 是 'man' 且 'alive' 是 'no' 的乘客。

79. 筛选出 'fare' 在 50 到 100 之间的所有行。

80. 筛选出独自一人 ('alone' 列为 True) 且幸存 ('survived' 为 1) 的乘客。

81. 筛选出所有名字 (who) 中包含 "woman" 的乘客 (提示: 使用 .str.contains())。

82. 筛选出年龄在20岁以下或60岁以上的所有乘客。

83. 筛选出不是从 'S' 港口登船的乘客。

84. 筛选出 'fare' 高于所有乘客平均 'fare' 的记录。

85. 筛选出 'age' 小于 18 岁且 'pclass' 不是 3 的乘客。

86. 使用 query() 方法实现与问题71相同的筛选。

87. 使用 query() 方法实现与问题73相同的筛选。

88. 筛选出 titanic DataFrame 中索引为奇数的行。

89. 筛选出 'sibsp' 和 'parch' 两列值都为 0 的乘客。

设置值 (91-100)

91. 将 titanic 第 0 行 'age' 列的值设置为 100。

92. 将所有 'pclass' 为 3 的乘客的 'fare' 设置为平均票价。

93. 将所有 'sex' 为 'female' 的乘客的 'age' 增加 5 岁。

94. 使用 .loc 将索引为 0 到 10 的 'embarked' 列设置为 'N/A'。

95. 使用布尔索引，将所有年龄小于1岁的婴儿年龄更新为1。

96. 新增一列 'age_group'，对于年龄小于18岁的设为 'child'，否则为 'adult'。

97. 将 'fare' 低于10的乘客的 'pclass' 全部修改为3。

98. 将 DataFrame 中所有 'age' 的缺失值填充为 0。

99. 将所有从 'S' 登船的女性乘客的 'fare' 翻倍。

100. 使用 .iloc 将 DataFrame 右下角的单元格值设为 999。
[[分析客户]]

---
==中文的￥和日文的￥进行区分，容易糊涂，排查很久，最后找出来了==
### **模块四：数据清洗 (40题)**

**核心知识点**:

- 处理缺失值 (`NaN`)：检测、删除、填充。
    
- 处理重复数据。
    
- 数据类型转换。
    
- 字符串处理。
    

准备数据集:

创建一个包含缺失值、重复和混合数据类型的数据集。

Python

```
import pandas as pd
import numpy as np

data = {'name': ['Alice', 'Bob', 'Charlie', 'David', 'Alice', 'Eva', np.nan],
        'age': [25, 30, np.nan, 22, 25, 28, 35],
        'city': ['New York', 'Los Angeles', 'Chicago', 'New York', 'New York', 'Los Angeles', 'Chicago'],
        'salary': ['50000', '65000', '48000', '52000', '50000', '70000', '61000']}
df_clean = pd.DataFrame(data)
```

**练习题**:

缺失值处理 (101-120)

101. 检查 df_clean 中每列的缺失值数量。

102. 使用 isnull() 和 sum() 找出含有缺失值的列。

103. 使用 notnull() 筛选出 'name' 列不含缺失值的行。

104. 删除所有包含缺失值的行。

105. 删除所有值都为缺失值的行（如果存在）。

106. 删除 'age' 列中包含缺失值的行。

107. 删除缺失值超过2个的列（如果存在）。

108. 将 'age' 列的缺失值用该列的平均值填充。

109. 将 'name' 列的缺失值用字符串 'Unknown' 填充。

110. 使用前一个非缺失值填充 'age' 列的缺失值（向前填充）。

111. 使用后一个非缺失值填充 'age' 列的缺失值（向后填充）。

112. 使用 fillna 为 'age' 和 'name' 列同时指定不同的填充值。

113. 使用 interpolate() 方法对 'age' 列的缺失值进行线性插值填充。

114. 检查 df_clean 中是否还有任何缺失值。

115. 将所有缺失值替换为 -1。

116. 在 dropna 中使用 thresh 参数，保留至少有3个非缺失值的行。

117. 在 fillna 中使用 limit 参数，只向前填充1个缺失值。

118. 计算 df_clean 中缺失值的总数。

119. 计算 df_clean 中缺失值占总数据量的百分比。

120. 创建一个布尔掩码，标记出 df_clean 中缺失值的位置。

重复值处理 (121-125)

121. 检查 df_clean 中是否存在重复行。

122. 找出 df_clean 中的所有重复行。

123. 删除 df_clean 中的所有重复行，保留第一次出现的记录。

124. 删除 df_clean 中的所有重复行，保留最后一次出现的记录。

125. 基于 'name' 和 'city' 两列来判断和删除重复行。

数据类型转换与字符串处理 (126-140)

126. 查看 df_clean 中 'salary' 列的数据类型。

127. 将 'salary' 列的数据类型从 object 转换为 int。

128. 将 'age' 列的数据类型转换为 float。

129. 如果转换失败，使用 pd.to_numeric 并设置 errors='coerce' 将无效值转为 NaN。

130. 将 'age' 列（填充缺失值后）的数据类型转换为 int。

131. 将 'name' 列的所有字符串转换为大写。

132. 将 'city' 列的所有字符串转换为小写。

133. 移除 'salary' 列字符串中的所有逗号（假设存在）。

134. 将 'name' 列中所有名为 'Alice' 的替换为 'Alicia'。

135. 获取 'city' 列每个字符串的长度，并存入新列 'city_len'。

136. 检查 'name' 列是否包含字母 'a'。

137. 拆分 'city' 列（假设格式为 "City, State"），并创建新列。

138. 在 'name' 列的每个名字前添加 "Mr. " 前缀。

139. 去除 'name' 列字符串两边的空白字符（如果存在）。

140. 将 'city' 列中的 'New York' 替换为 'NY'。

---

### **模块五：数据运算与函数应用 (30题)**

**核心知识点**:

- DataFrame/Series 间的算术运算。
    
- 使用 `.apply()`, `.map()`, `.applymap()` 对数据应用自定义函数。
    

准备数据集:

继续使用 titanic 数据集和自定义的 df_ops。

Python

```
import pandas as pd
import numpy as np
import seaborn as sns

titanic = sns.load_dataset('titanic')
df_ops = pd.DataFrame(np.random.randint(0, 100, size=(5, 3)), columns=['A', 'B', 'C'])
```

**练习题**:

基本运算 (141-150)

141. 在 df_ops 中，将 'A' 列的所有值加 10。

142. 在 df_ops 中，计算 'A' 列和 'B' 列的和，并存入新列 'A_plus_B'。

143. 在 df_ops 中，计算每行的平均值。

144. 从 df_ops 的每一列中减去该列的平均值。

145. 在 titanic 数据集中，计算 'sibsp' 和 'parch' 的和，创建新列 'family_size'。

146. 计算 df_ops 中每一列的累计和。

147. 在 df_ops 中找出每行的最大值。

148. 计算 titanic 数据集中 'fare' 列的对数。

149. 将 df_ops 中的所有元素乘以2。

150. 计算 df_ops 中 'A' 列除以 'B' 列的结果（注意处理除以0的情况）。

函数应用 (151-170)

151. 使用 .apply() 计算 df_ops 每列的元素范围（最大值-最小值）。

152. 编写一个函数，输入一个数，如果大于50则返回 'high'，否则返回 'low'，并将其应用到 df_ops 的 'A' 列。

153. 使用 .apply() 和 lambda 函数，计算 titanic 中每位乘客名字的长度。

154. 使用 .applymap() 将 df_ops 中所有元素格式化为字符串，如 "Value: x"。

155. 在 titanic 的 'sex' 列上使用 .map()，将 'male' 映射为 1，'female' 映射为 0。

156. 使用 .apply() 对 titanic 的 'age' 和 'fare' 两列计算它们的乘积。

157. 编写一个函数，返回一个 Series 的基本统计信息（均值，标准差），并将其应用到 df_ops 的每一列。

158. 使用 lambda 函数和 .apply()，对 titanic 的 'fare' 列进行归一化 (x - min) / (max - min)。

159. 使用 .apply() 统计 titanic 每一行中缺失值的数量。

160. 对 'pclass' 列使用 .map()，将其映射为描述性字符串 {1: 'First', 2: 'Second', 3: 'Third'}。

161. 将一个自定义的平方函数应用到 df_ops 的所有元素上。

162. 使用 lambda 函数和 apply，按行计算 df_ops 中 'A' 和 'C' 列的差的绝对值。

163. 对 titanic DataFrame 应用一个函数，该函数将所有数值列的值都乘以100。

164. 使用 .map() 处理 'age' 列，将 NaN 映射为 -1，其他值保持不变。

165. 对 df_ops 使用 .applymap()，将所有偶数替换为0，奇数替换为1。

166. 对 titanic DataFrame 使用 .pipe() 方法链式调用一个自定义函数（例如，一个填充缺失值的函数）。

167. 编写一个函数，如果一行中 'A' 列的值大于 'B' 列，则返回 True，并将其应用到 df_ops 的每一行。

168. 使用 lambda 和 .apply()，从 'name' 列中提取乘客的姓氏 (提示: 按逗号分割)。

169. 将一个返回多个值的函数（如返回最小值和最大值的元组）应用到 df_ops 的每一列。

170. 使用 .map() 和一个字典，快速转换 titanic 中 'embarked' 列的缩写为全称。

---

### **模块六：分组与聚合 (40题)**

**核心知识点**:

- 使用 `.groupby()` 进行数据分组。
    
- 对分组后的数据进行聚合操作，如 `sum()`, `mean()`, `count()`, `agg()`。
    
- 分组后的转换 (`transform`) 和过滤 (`filter`)。
    

准备数据集:

继续使用 titanic 数据集。

练习题:

171. 按 'sex' 列对 titanic 数据进行分组。

172. 计算每个性别的乘客的平均年龄。

173. 计算每个 'pclass' 等级中乘客的幸存率。

174. 按 'sex' 和 'pclass' 进行多重分组，并计算每组的乘客数量。

175. 计算每个登船港口 ('embarked') 的平均票价 ('fare')。

176. 找出每个 'pclass' 中年龄最大和最小的乘客。

177. 使用 .agg() 对按 'pclass' 分组的数据同时计算 'age' 的均值和 'fare' 的总和。

178. 计算每个 'pclass' 中幸存和未幸存的人数。

179. 按 'who' 列分组，并获取每组的 'fare' 的基本描述统计。

180. 按 'pclass' 分组，并列出每组的索引。

181. 遍历按 'sex' 分组后的每个组。

182. 获取 'pclass' 为 1 的那个分组。

183. 按 'deck' 分组，并计算每层甲板的乘客幸存率，然后按幸存率降序排序。

184. 使用 .agg() 对按 'pclass' 分组后的 'age' 列同时计算均值、中位数和标准差。

185. 对按 'sex' 分组后的数据，计算 'fare' 列的均值，并重命名聚合后的列为 'mean_fare'。

186. 按 'pclass' 分组，然后计算每组内 'age' 列的缺失值数量。

187. 按 'embarked' 和 'sex' 分组，计算幸存人数的总和。

188. 使用 size() 计算每个 'pclass' 分组的大小。

189. 使用 count() 计算每个 'pclass' 分组中非空的 'age' 值的数量。size() 和 count() 有何区别？

190. 按 'pclass' 分组，并对 'fare' 列应用一个自定义的聚合函数（如计算极差）。

191. 使用 .transform() 计算每个 'pclass' 的平均年龄，并将其作为一个新列添加到原 DataFrame 中。

192. 使用 lambda 函数和 .transform()，将每个 'pclass' 分组的 'fare' 进行中心化处理（减去组内均值）。

193. 使用 .filter() 筛选出那些平均年龄大于 30 岁的 'pclass' 分组。

194. 使用 lambda 和 .filter()，筛选出乘客数量超过 100 人的 'embarked' 分组。

195. 对按 'sex' 分组的数据，获取每组的前3条记录。

196. 按 'pclass' 分组，并对 'fare' 列进行排名。

197. 计算每个 'pclass' 中 'alone' 状态为 True 和 False 的人数。

198. 对 titanic 数据集，按 'sex' 分组计算幸存率，并找出幸存率更高的一组。

199. 按年齡段（如0-18, 18-60, 60+）进行分组，计算各年龄段的幸存率。

200. 按 'family_size' (需要先创建此列) 分组，找出最大家庭规模。

201. 使用 get_group() 方法获取 'sex' 分组中 'female' 组的 DataFrame。

202. 对 'pclass' 和 'sex' 分组，计算每组 'fare' 的总和、均值和数量。

203. 按 'sibsp' 分组，筛选出平均 'fare' 高于50的分组。

204. 对按 'embarked' 分组的数据，将 'age' 的缺失值用该组的 'age' 中位数填充。

205. 按 'deck' 分组，计算每组的 'fare' 中位数与均值之差。

206. 按 'pclass' 分组，找出每组中 'fare' 最高的记录。

207. 使用 nunique() 统计每个 'pclass' 中有多少个不同的 'embarked' 地点。

208. 将多个聚合函数应用于不同的列，例如按 'pclass' 分组，计算 'age' 的均值和 'fare' 的和。

209. 按 'who' 列分组，计算每个组内 'fare' 与该组平均 'fare' 的差值。

210. 使用 .filter() 筛选出 'fare' 方差小于1000的 'pclass' 组。

---

### **模块七：数据合并、连接与拼接 (30题)**

**核心知识点**:

- `pd.concat()`: 沿某个轴将多个对象堆叠到一起。
    
- `pd.merge()`: 类似数据库的 `JOIN` 操作，根据一个或多个键将不同的 DataFrame 连接起来。
    
- `DataFrame.join()`: 一个方便的、基于索引的合并方法。
    

**准备数据集**:

Python

```
import pandas as pd

df1 = pd.DataFrame({'A': ['A0', 'A1', 'A2', 'A3'],
                    'B': ['B0', 'B1', 'B2', 'B3'],
                    'key': ['K0', 'K1', 'K2', 'K3']})

df2 = pd.DataFrame({'C': ['C0', 'C1', 'C2', 'C3'],
                    'D': ['D0', 'D1', 'D2', 'D3'],
                    'key': ['K0', 'K1', 'K2', 'K4']})

df3 = pd.DataFrame({'E': ['E0', 'E1'],
                    'F': ['F0', 'F1']},
                   index=['K0', 'K1'])
```

**练习题**:

拼接 (Concatenation) (211-220)

211. 沿行方向（axis=0）拼接 df1 和 df2 的前两行。

212. 沿列方向（axis=1）拼接 df1 和 df2。

213. 拼接 df1 和 df2，并忽略原有的索引，生成新的连续索引。

214. 拼接 df1 和 df2，并为它们创建多级索引以区分来源。

215. 将 df2 的第一行作为一个 DataFrame 拼接到 df1 的末尾。

216. 定义一个新的 DataFrame df_new 并将其与 df1 沿列方向拼接。

217. 拼接两个 Series 对象。

218. 使用 append() 方法将 df2 的一行追加到 df1 (注意: append 未来可能被弃用)。

219. 拼接时，只保留两个 DataFrame 中共有的列（join='inner'）。

220. 拼接 df1 和一个只有一列但索引不同的 DataFrame。

合并 (Merging) (221-235)

221. 使用 pd.merge() 对 df1 和 df2 进行内连接（inner join）。

222. 对 df1 和 df2 进行左连接（left join）。

223. 对 df1 和 df2 进行右连接（right join）。

224. 对 df1 和 df2 进行外连接（outer join）。

225. 在合并 df1 和 df2 时，使用 indicator=True 来显示每行的合并来源。

226. 假设 df1 和 df2 的连接键列名不同，如何指定 left_on 和 right_on 进行合并？

227. 假设有两个 DataFrame，需要基于它们的索引进行合并，如何操作？

228. 合并 df1 和 df2，但如果出现重名列（非连接键），为它们添加自定义后缀。

229. 合并两个有多个连接键的 DataFrame。

230. 使用 validate 参数检查合并操作是否符合预期（如 "one_to_one"）。

231. 找出 df1 的 key 中存在但 df2 的 key 中不存在的记录。

232. 找出 df1 和 df2 中 key 相同的全部记录。

233. 对 df1 进行自连接（self-join）。

234. 合并 df1 和 df2，并按 'key' 列排序结果。

235. 使用 merge_asof 进行近似合并（例如，基于时间）。

连接 (Joining) (236-240)

236. 使用 df1 的 join() 方法连接 df3。join() 默认基于什么进行连接？

237. 将 df1 的 'key' 列设为索引，然后与 df3 进行连接。

238. 使用 join 实现 df1 和 df2 (以 key 为索引) 的左连接。

239. 如何使用 join 方法一次性连接多个 DataFrame？

240. merge 和 join 的主要区别是什么？在什么情况下你会选择使用 join？

---

### **模块八：数据透视表与重塑 (20题)**

**核心知识点**:

- `pivot_table()`: 创建类似电子表格的数据透视表。
    
- `stack()` 和 `unstack()`: 在层级化索引的 DataFrame 中进行列和索引的转换。
    
- `melt()`: 将宽格式数据转换为长格式数据。
    

准备数据集:

继续使用 titanic 数据集。

练习题:

241. 使用 pivot_table() 创建一个数据透视表，以 'sex' 为索引，'pclass' 为列，值为 'survived' 的均值。

242. 创建一个数据透视表，行索引为 'sex'，列索引为 'pclass'，聚合函数为 len (计算每组人数)。

243. 创建一个数据透视表，索引为 'sex' 和 'pclass'，值为 'fare' 的平均值。

244. 在问题241的基础上，使用 aggfunc 同时计算幸存率的均值和总人数。

245. 在数据透视表中，使用 margins=True 添加总计行和列。

246. 创建一个数据透视表，索引为 'embarked'，列为 'who'，值为 'fare' 的中位数，并用0填充缺失值。

247. 创建一个具有多级列索引的数据透视表（例如，pclass 和 alone）。

248. 创建一个 titanic 的子集，包含 'sex', 'pclass', 'survived'，然后将其 'sex' 和 'pclass' 设为多级索引。

249. 对上一步创建的多级索引 DataFrame 使用 unstack()，将 'pclass' 从索引“提升”到列。

250. 对上一步的结果使用 stack()，将其“压回”到索引中。

251. 使用 unstack(level=0) 将第一级索引提升到列。

252. 创建一个简单的宽格式 DataFrame (例如，每个学生一排，各科成绩为列)。

253. 使用 melt() 将上一步的宽格式数据转换为长格式（'学生', '科目', '分数'）。

254. 在 melt() 中，使用 id_vars 指定要保持不变的标识符变量。

255. 在 melt() 中，使用 value_vars 指定要“融化”的列。

256. 使用 pd.crosstab() 计算 'sex' 和 'pclass' 之间的频数表。

257. 使用 pd.crosstab() 计算 'sex' 和 'survived' 的交叉表，并进行归一化处理以显示百分比。

258. 将 titanic 数据集按 'pclass' 分组后，使用 unstack() 快速创建一个透视表。

259. pivot_table 和 groupby 有何异同？

260. 使用 melt 将 titanic 的 'pclass' 和 'sex' 列融化，'age' 作为值。

---

### **模块九：时间序列分析 (25题)**

**核心知识点**:

- 创建和处理时间序列数据。
    
- 时间索引 (`DatetimeIndex`) 的使用。
    
- 重采样 (`resample`)、窗口函数 (`rolling`)。
    
- 时间位移和频率转换。
    

**准备数据集**:

Python

```
import pandas as pd

# 创建一个时间序列
date_rng = pd.date_range(start='2025-01-01', end='2025-03-31', freq='D')
ts = pd.DataFrame(date_rng, columns=['date'])
ts['data'] = np.random.randint(0, 100, size=(len(ts)))
ts.set_index('date', inplace=True)
```

练习题:

261. 将一个包含日期字符串的列转换为 datetime 对象。

262. 从 ts 中选择2025年1月份的所有数据。

263. 选择 ts 中 '2025-02-10' 到 '2025-02-20' 之间的数据。

264. 将 ts 的数据按周 ('W') 进行重采样，并计算每周数据的总和。

265. 将 ts 的数据按月 ('M') 进行重采样，并计算每月数据的平均值。

266. 对 ts 的数据进行下采样到季度 ('Q')，并找出每个季度的最大值。

267. 对 ts 的数据进行上采样到6小时 ('6H')，并使用向前填充方法填充缺失值。

268. 计算 ts 数据的7天移动平均值。

269. 计算 ts 数据的10天滚动窗口中的最大值。

270. 计算 ts 数据的累积和。

271. 从 ts 的索引中提取年份、月份和星期几。

272. 将 ts 的索引向前移动1天。

273. 计算 ts 数据与前一天数据之间的差值。

274. 筛选出 ts 中所有星期一的数据。

275. 将 ts 的索引转换为周期（Period）类型。

276. 计算 ts 数据的指数加权移动平均（EWMA）。

277. 将本地化的时间序列转换为不同的时区。

278. 使用 asfreq 将时间序列转换为指定的频率，不进行聚合。

279. 找出 ts 数据中每个月的最后一个工作日的数据。

280. 计算每个月的第一个和最后一个数据点之间的差值。

281. 对 ts 数据应用一个滚动窗口，并在每个窗口上应用一个自定义的聚合函数。

282. 在 rolling 中使用 center=True 使窗口中心对齐。

283. 将 ts 的数据按周进行重采样，并同时计算总和、均值和标准差。

284. 筛选出 ts 中所有季度末的数据。

285. 从 ts 数据中减去其3天移动平均值，以获得一个去趋势的序列。

---

### **模块十：数据导入与导出 (15题)**

**核心知识点**:

- 从各种常见文件格式（CSV, Excel, JSON, SQL）读取数据到 DataFrame。
    
- 将 DataFrame 写入到这些文件格式中。
    

准备数据集:

我们将使用之前创建的 titanic DataFrame，并手动创建一些模拟文件内容。

练习题:

286. 将 titanic DataFrame 保存为一个名为 'titanic.csv' 的 CSV 文件。

287. 保存 'titanic.csv' 时，不包含索引列。

288. 从 'titanic.csv' 文件中读取数据到新的 DataFrame。

289. 读取 CSV 文件时，指定某一列作为索引。

290. 将 titanic DataFrame 的前100行保存到一个 Excel 文件 'titanic.xlsx' 中。

291. 将 df1 和 df2 保存到同一个 Excel 文件的不同工作表 (Sheet) 中。

292. 从 'titanic.xlsx' 文件中读取数据。

293. 读取 Excel 文件时，只读取指定的工作表。

294. 将 titanic DataFrame 转换为 JSON 字符串。

295. 将该 JSON 字符串保存到 'titanic.json' 文件中。

296. 从 'titanic.json' 文件中读取数据。

297. 将 titanic DataFrame 导出为 HTML 表格。

298. (理论题) 描述如何使用 pd.read_sql 从数据库中读取数据。

299. (理论题) 描述如何使用 df.to_sql 将 DataFrame 写入到数据库表中。

300. 将 titanic DataFrame 复制到系统剪贴板，以便粘贴到其他应用程序中。

---

祝您学习愉快！建议您按模块顺序练习，动手实践是掌握 Pandas 的唯一途径。
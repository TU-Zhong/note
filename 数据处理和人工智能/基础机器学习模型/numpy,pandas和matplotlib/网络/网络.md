好的，为了帮助您系统地将**计算机网络理论**与**Java网络编程实战**相结合，我为您精心设计了这套学习路径，包含 **10个核心知识点** 和配套的 **250道实战与思考题**。

这个计划旨在让您从底层的TCP/UDP协议起步，经过传统的BIO、NIO，最终精通高性能的Netty框架以及复杂的网络故障排查。每一部分不仅要求您懂理论，更要求您能用Java代码实现或验证。

### **学习结构**

1. **核心知识点 (Core Concepts)**: 对每个关键网络层级和Java网络API进行简要梳理。
    
2. **准备环境 (Environment Setup)**: 推荐的工具、JDK版本或测试依赖。
    
3. **练习题 (Practice Questions)**: 围绕理论验证、API调用、框架构建的实战任务，共250题。
    

### **准备工作：环境与工具**

在开始之前，请确保您的开发环境准备就绪：

- **JDK版本**: 建议使用 JDK 11 或更高版本（为了使用原生的 `java.net.http.HttpClient`）。
    
- **依赖管理**: Maven 或 Gradle（后续模块需要引入 Netty、Protobuf 等库）。
    
- **诊断工具**: 安装 Wireshark（抓包分析）、Postman/curl（接口测试）、Netcat (`nc` 命令，用于快速建立TCP连接测试）。
    

### **模块一：Java 网络底层 API 与 传输层基础 (25题)**

**核心知识点**:

- IP地址与端口的抽象。
    
- TCP协议的可靠传输（三次握手、四次挥手）在代码中的体现。
    
- UDP协议的无连接特性。
    
- `java.net` 包下的 `InetAddress`, `Socket`, `ServerSocket`, `DatagramSocket`。
    

**准备环境**: 无需额外依赖，纯 Java SE。

**练习题**:

**IP与网络接口 (1-5)**

1. 使用 `InetAddress.getLocalHost()` 获取本机的IP地址和主机名。
    
2. 使用 `InetAddress.getByName()` 查询 "[www.github.com](https://www.github.com/)" 的所有IP地址。
    
3. 编写代码，通过 `NetworkInterface` 枚举本机所有的物理和虚拟网卡及其绑定的IP。
    
4. 验证一个给定的字符串是否是合法的 IPv4 地址。
    
5. 测试 `InetAddress.isReachable(timeout)` 方法，模拟简单的 Ping 功能。
    

**TCP Socket 编程基础 (6-15)**

6. 编写一个基础的 `ServerSocket`，绑定在 8080 端口，并在 `accept()` 处阻塞等待。

7. 编写一个客户端 `Socket`，连接本机的 8080 端口。

8. 实现一个基于 TCP 的 Echo Server：服务端接收到客户端发送的字符串后，原样返回。

9. 修改 Echo 客户端，使其能够不断从控制台读取输入并发送给服务端，直到输入 "exit"。

10. 通过捕获异常，模拟并观察客户端连接一个未开启的端口时抛出的 `ConnectException: Connection refused`。

11. 观察 `SocketTimeoutException`：为 `ServerSocket` 设置 `setSoTimeout()`，验证超时行为。

12. 编写代码测试 TCP 的全双工通信：客户端和服务端各自开启两个线程，一个专门读，一个专门写。

13. 关闭连接实验：客户端调用 `socket.close()` 后，服务端尝试读取数据，观察返回的结果（应返回 -1）。

14. 半关闭连接实验：客户端调用 `socket.shutdownOutput()`，服务端仍能将剩余数据读完，且客户端还能接收服务端的响应。

15. 编写一个简单的端口扫描器，多线程扫描局域网内某台机器的 1-1024 端口哪些是开放的。

**UDP Socket 编程基础 (16-25)**

16. 创建一个 `DatagramSocket`，绑定到 9000 端口，准备接收数据。

17. 使用 `DatagramPacket` 封装一段字符串，并通过 UDP 发送到 9000 端口。

18. 实现一个基于 UDP 的 Echo Server，解析接收到的 packet 的来源 IP 和 端口，并将数据原路发回。

19. 编写 UDP 客户端，发送数据后设置超时时间，如果在规定时间内没收到响应则重发（模拟应用层可靠性）。

20. 测试 UDP 的边界保留特性：发送端连续发送两个 packet，接收端使用两次 `receive()` 接收，验证不会发生“粘包”。

21. 编写代码加入一个多播组 (MulticastGroup)，使用 `MulticastSocket` 接收多播消息。

22. 编写多播发送端，向多播地址（如 224.0.0.1）发送一条消息，并用多个接收端验证。

23. 验证 UDP 广播：向局域网广播地址（如 255.255.255.255）发送消息。

24. 设置 UDP 套接字的缓冲区大小 `setReceiveBufferSize()`，并发送超过该大小的数据报，观察丢包情况。

25. 比较：在本地回环地址 (127.0.0.1) 下，分别高频发送 10000 条 TCP 和 UDP 消息，记录时间差异。

### **模块二：传统阻塞 I/O (BIO) 与并发模型演进 (25题)**

**核心知识点**:

- BIO 的阻塞特性：`read()`, `write()`, `accept()` 是如何阻塞线程的。
    
- 每连接一线程模型 (Thread-per-Connection) 的实现与瓶颈。
    
- 线程池在网络编程中的应用与伪异步 I/O。
    
- 流的操作：`InputStream`, `OutputStream`, 字符流与缓冲流的结合。
    

**练习题**:

**流与数据处理 (26-33)**

26. 通过 `Socket.getInputStream()` 获取字节流，并使用 `InputStreamReader` 将其转换为字符流。

27. 结合 `BufferedReader` 的 `readLine()` 方法按行读取 Socket 传入的数据。

28. 为什么在使用 `readLine()` 时，发送端必须在数据末尾加上 `\n` 或 `\r\n`？（理论思考题并用代码验证不加的后果）。

29. 使用 `DataOutputStream` 将 Java 的基本数据类型（如 int, long, double）直接写入 Socket。

30. 使用 `DataInputStream` 在接收端准确无误地还原上一步发送的各种基本数据类型。

31. 使用 `ObjectOutputStream` 和 `ObjectInputStream` 在网络间传输实现了 `Serializable` 接口的 Java 对象。

32. 模拟大文件传输：客户端通过 TCP 向服务端发送一个 100MB 的文件，分块读取并写入。

33. 在文件传输过程中计算并打印传输进度（百分比）。

**并发模型演进 (34-50)**

34. 测试单线程 BIO Server：当一个客户端连接并保持不发数据时，第二个客户端的连接请求会发生什么？

35. 实现 Thread-per-Connection 模型：每当 `accept()` 一个新 Socket，就 `new Thread()` 去处理该连接。

36. 在高并发下测试上一步的模型，使用压测工具（或自己写多线程客户端）发起 5000 个连接，观察 JVM 内存和线程数变化。

37. 重构代码，引入 `ExecutorService`（线程池）来处理客户端连接，实现伪异步 I/O。

38. 测试线程池模型的上限：设置核心线程数为10，最大线程数为20，阻塞队列长度为50。当发起100个并发连接时，分析哪些连接会被拒绝。

39. 自定义 `RejectedExecutionHandler`，当线程池满时，向客户端返回 "Server Busy" 提示后再关闭连接。

40. 实现一个连接管理器，记录当前所有活跃的 Socket 连接，并支持向所有活跃连接群发消息（简单的聊天室逻辑）。

41. 处理客户端意外断开：在读取数据时捕获 `EOFException` 或 `SocketException`，并清理资源。

42. 编写心跳检测（服务端侧）：开启一个定时任务，每隔10秒检查活跃连接列表中各连接的最后一次通信时间。

43. 踢出空闲连接：对于上一步中超过30秒未发送数据的连接，服务端主动调用 `close()`。

44. 编写心跳机制（客户端侧）：空闲时每隔5秒向服务端发送一个特殊的 Ping 消息。

45. 思考并验证：BIO 中，如果服务端网络线缆被直接拔掉（不发 FIN），客户端的 `read()` 会立刻返回吗？为什么？

46. 使用 `Socket.setSoLinger(true, 0)` 测试 TCP 强制关闭（发送 RST 包代替 FIN），通过 Wireshark 抓包验证。

47. 实现简单的命令路由：服务端接收请求，根据请求的第一行决定调用哪个处理类（如 "GET_TIME" 返回时间，"ECHO" 原样返回）。

48. 限制单个 IP 的最大连接数：在 `accept()` 后检查请求来源 IP，如果超过阈值则立即关闭。

49. 统计并打印服务端的 QPS（每秒处理请求数）和实时连接数。

50. (理论总结) 总结 BIO 模型的致命缺点，并解释为什么需要引入非阻塞 I/O (NIO)。

### **模块三：应用层核心：HTTP/1.1 协议与 Java 实战 (25题)**

**核心知识点**:

- HTTP 报文结构（请求行、请求头、空行、请求体；状态行、响应头、响应体）。
    
- 常见状态码、请求方法、Header 属性。
    
- 使用原生 Socket 手写 HTTP Server/Client。
    

**练习题**:

**手写 HTTP 解析器 (51-60)**

51. 使用原生的 BIO `ServerSocket` 监听 80 端口，并用浏览器访问，打印浏览器发来的原始 HTTP 请求报文。

52. 编写代码解析 HTTP 请求行，提取出 Method (GET/POST)、URI 和 HTTP 版本。

53. 编写代码解析 HTTP 请求头 (Headers)，将其存储到一个 `Map<String, String>` 结构中。

54. 处理 GET 请求：根据解析出的 URI，读取本地对应目录下的 HTML 文件内容。

55. 构造 HTTP 响应报文：手动拼接状态行（如 `HTTP/1.1 200 OK\r\n`）、响应头和空行，然后输出 HTML 内容。

56. 构造 404 响应：当请求的本地文件不存在时，返回 `HTTP/1.1 404 Not Found` 状态码及相应的错误页面。

57. 解析 POST 请求体：当 Header 中包含 `Content-Length: X` 时，根据 X 的大小准确读取请求体的内容。

58. 实现简单的 Content-Type 识别：请求图片时返回 `Content-Type: image/jpeg`，请求文本时返回 `text/html`。

59. 结合浏览器测试：确保你的手写服务器能在浏览器中正确渲染一个包含 CSS 和图片的复杂 HTML 页面。

60. 解析 Query Parameter：从 URI 中解析出 `?key1=value1&key2=value2` 的参数对放入 Map。

**HTTP 核心机制验证 (61-75)**

61. 验证 `Keep-Alive`：观察默认情况下，浏览器与你的手写 Server 是否复用同一个 TCP 连接发送后续资源的请求。

62. 实现连接复用：修改 Server 逻辑，处理完一个请求后不关闭 Socket，而是继续在 `while` 循环中等待下一个 HTTP 报文。

63. 解析并验证 Cookie：提取请求头中的 `Cookie` 字段，并在响应中加入 `Set-Cookie: sessionid=12345`，用浏览器观察。

64. 实现基本认证 (Basic Authentication)：检查请求头中的 `Authorization`，若无或错误则返回 `401 Unauthorized` 和对应的 `WWW-Authenticate` 头。

65. 实现 301/302 重定向：根据特定 URI 返回 302 状态码和 `Location: https://www.google.com` 头。

66. 使用 `HttpURLConnection` 发起一个标准的 GET 请求并读取响应体。

67. 使用 `HttpURLConnection` 发起一个携带 JSON 数据的 POST 请求（设置 `Content-Type: application/json`）。

68. 处理连接超时和读取超时：使用 `setConnectTimeout` 和 `setReadTimeout`，并捕获相应的异常。

69. 实现客户端下载文件功能：发起请求，读取响应流，并将其写入本地文件系统。

70. 获取 HTTP 响应状态码及所有的响应 Header。

71. (思考与抓包) HTTP 协议的无状态特性是什么意思？Session 是如何通过 Cookie 解决无状态问题的？

72. 验证分块传输编码 (Chunked Transfer Encoding)：编写 Server 返回数据时不提供 `Content-Length`，而是设置 `Transfer-Encoding: chunked` 并按块发送。

73. 模拟 HTTP 范围请求 (Range Request)：客户端发送 `Range: bytes=0-100`，服务端解析并仅返回前 100 个字节，状态码 206。

74. 通过 `HttpURLConnection` 提交 multipart/form-data 格式的数据（模拟文件上传）。

75. 使用 Wireshark 抓包，完整分析一次 HTTP POST 请求从 DNS 解析、TCP 握手、HTTP 报文传输到 TCP 挥手的整个过程。

### **模块四：现代 Java 网络 API、HTTP客户端生态与 HTTP/2 (25题)**

**核心知识点**:

- Java 11 的 `java.net.http.HttpClient` API。
    
- 第三方优秀网络库（OkHttp, Apache HttpClient）。
    
- HTTP/2 的多路复用、头部压缩、Server Push 特性。
    

**准备环境**: Java 11+, 引入 OkHttp 依赖。

**练习题**:

**Java 11 HttpClient API (76-85)**

76. 使用 Java 11 的 `HttpRequest.newBuilder()` 构建一个 GET 请求，并通过 `HttpClient` 同步发送。

77. 使用 `HttpResponse.BodyHandlers.ofString()` 获取字符串格式的响应。

78. 使用 HttpClient 异步发送请求 (`sendAsync`)，并通过 `CompletableFuture.thenAccept` 处理回调结果。

79. 配置 HttpClient 的超时时间（使用 `connectTimeout`）和 HTTP 协议版本（强制使用 `Version.HTTP_2`）。

80. 通过 `HttpRequest.BodyPublishers.ofFile()` 实现优雅的文件上传。

81. 通过 `HttpResponse.BodyHandlers.ofFile()` 实现异步的大文件下载。

82. 结合 HttpClient 使用 Basic 认证，配置 `Authenticator`。

83. 编写代码，并行向 5 个不同的公共 API 发起异步请求，使用 `CompletableFuture.allOf()` 等待它们全部完成。

84. 自定义 `HttpClient` 的线程池执行器 (`executor`)，观察异步请求是在哪些线程中执行的。

85. 处理重定向：设置 HttpClient 的重定向策略为 `Redirect.ALWAYS`。

**OkHttp 框架实战 (86-95)**

86. 使用 OkHttp 的 `OkHttpClient` 和 `Request` 对象发起一个同步 GET 请求。

87. 使用 `Call.enqueue` 实现异步请求，并在 `Callback` 中处理响应。

88. 构造一个 `RequestBody`，并使用 OkHttp 发送 POST 请求提交 JSON 数据。

89. 使用 `MultipartBody.Builder` 发送包含文本参数和文件的表单数据。

90. 实现 OkHttp 拦截器 (Interceptor)：编写一个日志拦截器，打印所有发出的请求 URL 和响应状态码。

91. 编写一个重试拦截器：当请求遇到特定错误（如超时）时，自动重试最多 3 次。

92. 修改请求头拦截器：为所有的请求自动添加统一的 `Authorization: Bearer <token>` 头部。

93. 探讨并使用 OkHttp 的连接池 (`ConnectionPool`)，验证对同一域名的多次请求是否复用了底层 TCP 连接。

94. 开启 OkHttp 的缓存控制 (`Cache`)，将请求缓存到本地磁盘，断网后尝试加载已缓存的数据。

95. 解析 HTTP/1.1 的 Head-of-line Blocking (队头阻塞) 问题，它是发生在应用层还是传输层？

**HTTP/2 特性探索 (96-100)**

96. 使用 Java 11 HttpClient 强制建立 HTTP/2 连接，打印响应对象，确认协议版本 (`response.version()`)。

97. (理论测试) 简述 HTTP/2 的多路复用 (Multiplexing) 与 HTTP/1.1 Keep-Alive 的本质区别。

98. 什么是 HPACK 算法？它解决了什么问题？

99. 通过 Wireshark 抓包分析 HTTP/2 的底层二进制帧 (Frame)：找到 HEADERS 帧和 DATA 帧。

100. 简述 HTTP/3 为什么要抛弃 TCP 拥抱 QUIC (基于 UDP) 协议？

### **模块五：Java NIO (非阻塞 I/O) 深度探索 (25题)**

**核心知识点**:

- `Buffer` (缓冲区)、`Channel` (通道)、`Selector` (多路复用器)。
    
- Reactor 模式基础。
    
- 零拷贝 (Zero-Copy) 技术 (`transferTo`, `mmap`)。
    

**练习题**:

**Buffer 机制核心 (101-110)**

101. 分配一个容量为 1024 的 `ByteBuffer`，打印其初始的 `capacity`, `position`, `limit`。

102. 向 Buffer 中 `put` 5 个字节的数据，再次观察上述三个指针的变化。

103. 调用 `buffer.flip()` 方法，解释该方法的作用并观察指针变化，说明它如何为读操作做准备。

104. 使用 `buffer.get()` 依次读取 2 个字节，然后调用 `buffer.compact()`，分析其行为与 `clear()` 的区别。

105. 尝试在读模式下继续 `put` 数据，或者 `position` 超过 `limit` 时 `get` 数据，观察抛出的 `BufferOverflowException` 或 `BufferUnderflowException`。

106. 创建一个只读 Buffer：`buffer.asReadOnlyBuffer()`，验证对其写入时会发生什么。

107. 分配一个直接缓冲区 `ByteBuffer.allocateDirect()`，简述其与堆缓冲区 (`allocate`) 在内存分布和性能上的优劣。

108. 将一个字符串（如中文）通过特定字符集 (`Charset.forName("UTF-8").encode()`) 编码放入 ByteBuffer。

109. 将 ByteBuffer 中的字节通过同样的字符集 `decode()` 还原为字符串。

110. (坑点演练) 为什么网络编程中经常发生 ByteBuffer 遗漏了 `flip()` 导致的读取错误？写段代码演示这个错误。

**Channel 与 File IO (111-115)**

111. 使用 `FileChannel` 读取本地文件内容到 `ByteBuffer` 中。

112. 将多个 ByteBuffer 的数据同时写入到一个 Channel 中（Gathering Writes），测试 `channel.write(ByteBuffer[])`。

113. 从 Channel 读取数据并分散到多个 ByteBuffer 中（Scattering Reads），测试 `channel.read(ByteBuffer[])`。

114. 使用 `FileChannel.transferTo()` 和 `transferFrom()` 实现文件的极速拷贝。

115. (理论与实战) 上述 transfer 方法在底层对应了操作系统的什么技术？（零拷贝 sendfile），它的优势是什么？

**Selector 多路复用与网络非阻塞模型 (116-125)**

116. 打开一个 `ServerSocketChannel`，将其配置为非阻塞模式 `configureBlocking(false)`。

117. 打开一个 `Selector`，将 `ServerSocketChannel` 注册到 Selector 上，并监听 `SelectionKey.OP_ACCEPT` 事件。

118. 编写一个循环，调用 `selector.select()` 阻塞等待事件发生。

119. 遍历 `selector.selectedKeys()`，当触发 `isAcceptable()` 时，获取客户端的 `SocketChannel`。

120. 将获取到的客户端 `SocketChannel` 也设置为非阻塞，并注册到 Selector，监听 `OP_READ` 事件。

121. 当触发 `isReadable()` 时，从客户端 Channel 将数据读取到 ByteBuffer 并打印。

122. **核心坑点处理**：在迭代完 selectedKeys 之后，为什么一定要调用 `iterator.remove()`？不移除会怎样？

123. 处理客户端断开连接：在非阻塞读取时，如果 `read()` 返回 -1，如何正确取消 `SelectionKey` 并关闭 Channel。

124. (理论题) 解释 Linux 环境下 `select`, `poll`, `epoll` 的区别，Java NIO 底层在 Linux 上使用的是哪种机制？

125. 为什么说单线程 Reactor 模式（一个 Selector 处理所有 Accept 和 Read/Write）存在性能瓶颈？如何优化为多线程或主从 Reactor 模型？

### **模块六：高性能网络框架 Netty 核心基础 (25题)**

**核心知识点**:

- Netty 架构体系：EventLoop, Channel, ChannelPipeline, ChannelHandler。
    
- ByteBuf 的优势与内存管理。
    
- Bootstrap 启动与配置。
    

**准备环境**: 引入 `io.netty:netty-all` 依赖。

**练习题**:

**Netty 启动与基本组件 (126-135)**

126. 创建 `NioEventLoopGroup` 的 bossGroup 和 workerGroup，并解释它们各自的职责。

127. 使用 `ServerBootstrap` 组装服务端，绑定端口，并实现最简单的 Server 启动逻辑。

128. 实现一个 `ChannelInboundHandlerAdapter`，重写 `channelRead()` 方法，在服务端打印客户端发来的消息。

129. 编写客户端：使用 `Bootstrap` 和 `NioSocketChannel` 连接服务端。

130. 在客户端的 `channelActive()` 回调中向服务端发送一条 "Hello Netty" 的消息。

131. 使用 `ctx.writeAndFlush()` 实现服务端收到消息后立刻向客户端原样回传 (Netty版 Echo Server)。

132. 探索 `ChannelPipeline`：在服务端的 pipeline 中按顺序添加 3 个不同的入站(Inbound) Handler。

133. 在入站 Handler 中使用 `ctx.fireChannelRead()` 将消息显式传递给下一个 Handler，观察打印顺序。

134. 在 pipeline 中添加出站(Outbound) Handler，测试 `ctx.write()` 和 `ctx.channel().write()` 消息传递路径的不同。

135. 异常处理：重写 `exceptionCaught()` 方法，当网络发生异常或 Handler 抛出异常时关闭 `ChannelHandlerContext`。

**ByteBuf 的奥秘 (136-145)**

136. 分析 Netty 的 `ByteBuf` 相比 Java NIO 的 `ByteBuffer` 解决的痛点（如不需要显式 flip，自动扩容等）。

137. 测试 `ByteBuf` 的读写指针：`readerIndex` 和 `writerIndex`。

138. 调用 `buf.readByte()` 和 `buf.writeByte()`，观察指针的独立移动。

139. 创建池化内存 `PooledByteBufAllocator.DEFAULT.buffer()` 与非池化内存 `Unpooled.buffer()`，解释其适用场景。

140. 创建直接内存 (DirectBuffer) 和堆内存 (HeapBuffer)，并通过 `buf.hasArray()` 进行判断。

141. 测试 `ByteBuf` 的引用计数机制：调用 `buf.retain()` 增加引用，调用 `buf.release()` 减少引用。

142. (排错题) 为什么在 `channelRead` 的最后如果不传递消息，通常需要手动释放 `ByteBuf` 内存 (`ReferenceCountUtil.release(msg)`)？如果不释放会导致什么问题？

143. 测试 `SimpleChannelInboundHandler`：为什么继承它之后，我们不需要手动去 release `ByteBuf` 消息？

144. 零拷贝在 Netty 中的体现：使用 `CompositeByteBuf` 将两个小的 ByteBuf 逻辑上组合成一个，而不发生实际的内存拷贝。

145. 使用 `ByteBuf.slice()` 截取缓冲区的一段，验证它们是否共享底层同一块内存。

**异步回调机制 (146-150)**

146. `ChannelFuture` 解析：为什么 Netty 的网络操作（如 `bind`, `connect`, `write`）都是异步的？

147. 为 `connect()` 返回的 `ChannelFuture` 添加 `GenericFutureListener`，在连接成功或失败时打印日志。

148. 在服务端响应客户端后，实现响应完成后立即断开连接：`ctx.writeAndFlush(msg).addListener(ChannelFutureListener.CLOSE)`。

149. 使用 `EventLoop` 作为定时器：调用 `ctx.executor().schedule()` 在 5 秒后执行一项任务。

150. 理解 Netty 线程模型的核心铁律：为什么绝对不能在 `EventLoop` 线程（即 `ChannelHandler` 内部）中执行耗时的阻塞业务逻辑（如查数据库）？应该如何解决？

### **模块七：Netty 进阶应用与自定义协议实战 (25题)**

**核心知识点**:

- TCP 粘包与拆包问题的根本原因及 Netty 解决方案。
    
- 编解码器设计 (`MessageToByteEncoder`, `ByteToMessageDecoder`)。
    
- 空闲状态检测与心跳机制。
    
- 基于 Netty 开发 WebSocket 服务器。
    

**练习题**:

**粘包拆包与编解码 (151-160)**

151. 编写测试程序复现“粘包/半包”现象：客户端循环 100 次发送 "HelloWorld"，服务端观察收到的数据块是否完整。

152. 使用 Netty 内置的 `LineBasedFrameDecoder` 和 `StringDecoder` 解决按行 (`\n` 或 `\r\n`) 分割的粘包问题。

153. 使用 `FixedLengthFrameDecoder` 解决固定长度消息的粘包问题。

154. 使用终极方案 `LengthFieldBasedFrameDecoder`：基于消息头中包含长度字段的协议来解决粘包问题。

155. 自定义协议头结构：设计一个包含魔数(2字节)、版本号(1字节)、消息长度(4字节)和消息体(N字节)的协议。

156. 编写自定义解码器继承 `ByteToMessageDecoder`，解析上述自定义协议，处理半包（等待长度字段凑齐、等待消息体凑齐）。

157. 编写自定义编码器继承 `MessageToByteEncoder`，将 Java 对象封装为上述二进制协议格式发送。

158. 使用 Netty 提供的 `LoggingHandler` 在控制台打印网络数据的详细十六进制 dump，辅助协议调试。

159. 结合 `FastThreadLocal` 分析 Netty 如何优化多线程下的对象分配（深入底层机制）。

160. 对比 `ByteToMessageDecoder` 和 `ReplayingDecoder`，说明后者的便利性与性能代价。

**可靠性与高并发设计 (161-170)**

161. 在 pipeline 中引入 `IdleStateHandler`，配置读空闲 5秒，写空闲 7秒，所有空闲 10秒。

162. 编写 `HeartbeatHandler` 继承 `ChannelInboundHandlerAdapter`，重写 `userEventTriggered()`，当触发读空闲时主动断开连接。

163. 客户端实现断线重连：在 `channelInactive` 回调中，启动一个定时任务每隔 3 秒尝试重新连接 `Bootstrap.connect()`。

164. 服务端防御机制：当单机的 TCP 并发连接达到 10 万时，为了防止 OOM，如何限制全局的最大连接数？

165. 优化 Netty 配置：在 `ServerBootstrap` 启用 `ChannelOption.SO_BACKLOG` 设置 TCP 全连接队列的大小。

166. 启用 `ChannelOption.ALLOCATOR` 为 `PooledByteBufAllocator.DEFAULT`，确保全面使用池化内存。

167. 设置 `ChannelOption.TCP_NODELAY` 为 true，理解其关闭 Nagle 算法从而降低延迟的意义。

168. Netty 是如何处理 TCP 优雅关闭的 (Graceful Shutdown)？编写代码在进程退出前正确关闭 `EventLoopGroup`。

169. 分析高并发场景下：如何正确地使用自定义业务线程池处理 Handler 接收到的数据，以防阻塞 NIO 线程？

170. 使用 `Netty` 编写一个高吞吐的代理服务器 (Proxy Server) 骨架，读取客户端请求并转发到目标服务器。

**Netty 实现复杂应用层协议 (171-175)**

171. 使用 Netty 编写一个基础的 HTTP 服务器，引入 `HttpServerCodec` 编解码器。

172. 引入 `HttpObjectAggregator` 将分离的 HTTP 请求块 (请求行、头、体) 聚合成一个完整的 `FullHttpRequest` 对象。

173. 基于 Netty 实现 WebSocket 服务端：引入 `WebSocketServerProtocolHandler` 处理 WebSocket 握手。

174. 编写处理 `TextWebSocketFrame` 的 Handler，实现网页端向服务端的全双工实时聊天。

175. 测试 WebSocket 的 Ping/Pong 帧，实现底层的心跳保活。

### **模块八：RPC 原理、序列化与微服务网络架构 (25题)**

**核心知识点**:

- RPC (Remote Procedure Call) 的通信流程：动态代理 -> 序列化 -> 网络传输 -> 反序列化 -> 反射调用。
    
- 常用序列化协议对比 (JSON, Hessian, Protobuf)。
    
- 连接池与长连接多路复用。
    

**练习题**:

**序列化与反序列化 (176-185)**

176. 为什么 Java 原生的序列化方案 (Serializable) 不适合现代 RPC 框架？（从性能、跨语言、体积三方面思考）。

177. 使用 Jackson 库将一个复杂的 Java 实体类（包含枚举、时间类型）序列化为 JSON 字符串并反序列化。

178. (环境配置) 引入 Google Protocol Buffers (Protobuf) 编译器，编写一个 `.proto` 接口定义文件。

179. 使用 protoc 编译器将 `.proto` 文件生成对应的 Java 类。

180. 使用生成的 Protobuf Java 类，构建对象，并调用 `toByteArray()` 转化为字节数组，比较其与 JSON 的体积差异。

181. 在 Netty 的 pipeline 中集成 Protobuf 编解码器 (`ProtobufDecoder`, `ProtobufEncoder`, `ProtobufVarint32FrameDecoder`)。

182. 了解并测试 Kryo 序列化框架，对比其在深层对象嵌套情况下的性能优势。

183. 在序列化对象时增加“版本号”字段，探讨如何实现协议的向前兼容与向后兼容。

184. 什么是“序列化漏洞”？了解为何在反序列化外部传入的数据时，需要限制白名单机制。

185. 比较文本序列化 (JSON/XML) 和二进制序列化 (Protobuf/Thrift) 的可读性与解析效率。

**手写一个迷你 RPC 框架 (186-195)**

186. 定义一个公共的业务接口 `public interface HelloService { String sayHello(String name); }`。

187. 在服务端实现该接口。

188. 定义 RPC 请求消息体 `RpcRequest`，包含 className、methodName、parameterTypes 和 parameters。

189. 定义 RPC 响应消息体 `RpcResponse`，包含 requestId、error、result。

190. 编写客户端动态代理工厂：使用 `java.lang.reflect.Proxy` 拦截接口的所有方法调用。

191. 在代理内部：将拦截到的方法名、参数组装成 `RpcRequest` 对象，进行序列化。

192. 客户端利用 Netty 将序列化后的请求发送给服务端，并阻塞等待响应（或使用 CompletableFuture 实现异步等待）。

193. 服务端接收请求后：反序列化 `RpcRequest`，使用反射 (`Method.invoke`) 调用本地对应的实现类实例。

194. 服务端将执行结果封装入 `RpcResponse`，序列化后返回给客户端。

195. 客户端收到 `RpcResponse`，解除阻塞，返回具体结果。至此完成整个 RPC 闭环。

**网络架构与分布式通信设计 (196-200)**

196. 在 RPC 客户端实现一个简单的**连接池** (Commons Pool)，避免每次调用都重新建立 TCP 连接。

197. 长连接多路复用：为了彻底压榨单一长连接的性能，在 `RpcRequest` 中引入 `requestId` 机制，并发发送多个请求，依靠 `requestId` 将响应回调到对应的 Future 线程。

198. 什么是 API 网关 (Gateway)？用 Java 代码基于 Netty 模拟一个简单的网关，根据 URL 路径将请求反向代理到不同的后端端口。

199. 在客户端实现最简单的**负载均衡**策略：轮询算法 (Round Robin) 和随机算法 (Random) 选取服务器 IP。

200. 在网关处实现限流：使用 Guava 库的 `RateLimiter` 令牌桶算法，限制某个 IP 的最高请求频率。

### **模块九：网络安全：HTTPS与Java加密体系 (JCA/JSSE) (25题)**

**核心知识点**:

- 对称加密与非对称加密算法体系。
    
- 哈希算法与数字签名。
    
- TLS/SSL 握手过程。
    
- Java 的 KeyStore 和证书管理工具 (`keytool`)。
    

**练习题**:

**加解密基础 API (201-210)**

201. 使用 `MessageDigest` 类对一段字符串进行 MD5、SHA-256 哈希计算，并转换为十六进制字符串。

202. (理论防范) 为什么不能用单纯的 MD5 存储用户密码？实现基于盐值 (Salt) 的安全密码哈希存储 (如 PBKDF2 或 BCrypt)。

203. 使用 `Cipher` 类和 AES 算法，编写对称加密和解密的工具方法。

204. 测试 AES 的不同工作模式 (ECB, CBC) 和填充方式 (PKCS5Padding)，了解 IV (初始化向量) 在 CBC 模式下的重要性。

205. 生成一对 RSA 公私钥 (`KeyPairGenerator`)。

206. 使用 RSA 算法，利用公钥加密一段数据，再用私钥解密验证 (非对称加密)。

207. 使用 RSA 算法，利用私钥对一段数据的哈希值进行数字签名 (`Signature` 类)，并用公钥进行验签。

208. 对比 AES 和 RSA 的加密速度：分别加密 1MB 的数据，记录耗时。

209. 理解混合加密模型：设计一段逻辑，先用 RSA 交换一个随机的 AES 密钥，然后使用该 AES 密钥加密后续的大段业务数据。

210. 探讨 Base64 编码与加密的区别，编写代码使用 `java.util.Base64` 进行编码和解码。

**HTTPS 与证书实战 (211-225)**

211. 使用 JDK 自带的工具 `keytool` 在命令行生成一个自签名的 SSL 证书并存入 JKS 或 PKCS12 格式的 Keystore 文件。

212. 编写一个支持 HTTPS 的简单 Java Web Server（可以使用 JDK 内置的 `HttpsServer`），加载刚才生成的 Keystore 开启双向验证。

213. 编写 Java HttpClient 访问你本地的 HTTPS 服务，会抛出什么异常？（通常是证书不受信任）。

214. 编写一个自定义的 `TrustManager`，使其信任所有证书（仅用于测试环境开发，理解其危险性）。

215. 正确的做法：将服务器的自签名公钥证书导出，并导入到客户端的 Truststore 中，配置 SSLContext 进行合法访问。

216. 在 Netty 的 Pipeline 中添加 `SslHandler`，实现 Netty 的 SSL/TLS 通信加密。

217. (理论测试) 详述 TLS 1.2 的握手过程（Client Hello, Server Hello, Certificate, Key Exchange等）。

218. 什么是 SNI (Server Name Indication)？为什么在同一台服务器托管多个 HTTPS 域名时需要它？

219. (扩展阅读) 了解 TLS 1.3 相比 TLS 1.2 在握手延迟上的巨大优化 (0-RTT 和 1-RTT)。

220. 证书信任链：什么是根 CA，中间 CA？打开浏览器查看 github.com 的证书层级结构。

221. 如何吊销一个被泄露的证书？了解 CRL 和 OCSP 协议。

222. 使用 Java 的 `SecureRandom` 替代 `Random` 生成高强度的加密随机数，解释两者的底层熵源差异。

223. 防御重放攻击：在业务 API 报文设计中引入 Timestamp (时间戳) 和 Nonce (随机流水号)。

224. 在应用层实现简单的数据防篡改校验：针对业务参数按字典序排序拼接后加共享秘钥，计算 HMAC，附加在 Header 中。

225. 使用 Wireshark 抓取本地 HTTPS 通信包，观察 TLS 握手，并尝试通过配置 SSLKEYLOGFILE 变量让 Wireshark 解密加密的抓包数据。

### **模块十：网络抓包、诊断调优与故障排查 (25题)**

**核心知识点**:

- Wireshark 与 `tcpdump` 深入分析。
    
- TCP 状态机异常排查 (`CLOSE_WAIT`, `TIME_WAIT`)。
    
- 底层 Socket 参数调优。
    

**练习题**:

**抓包与协议深入分析 (226-235)**

226. 使用 Wireshark 抓取一次完整的 HTTP 访问，过滤出该 TCP 连接的数据流 (Follow TCP Stream)。

227. 观察 TCP 三次握手中的 SYN 和 ACK 包，找出双方协商的 MSS (Maximum Segment Size) 参数值。

228. 观察 Window Size (滑动窗口) 的变化，体会 TCP 是如何进行流量控制的。

229. 模拟弱网环境 (可使用相关限速或丢包工具)，在 Wireshark 中找出 TCP 的重传包 (Retransmission) 和重复 ACK (Duplicate ACK)。

230. 什么是 TCP 糊涂窗口综合征 (Silly Window Syndrome)？结合代码和抓包分析如何避免。

231. 学习在 Linux 服务器上使用 `tcpdump` 命令抓取特定端口的包，并保存为 `.pcap` 格式以便用 Wireshark 打开。

232. 执行 `netstat -anp | grep 8080` 或 `ss -tlnp`，查看服务器上的端口监听状态及对应的进程 PID。

233. 识别 SYN 泛洪攻击 (SYN Flood)：大量处于 `SYN_RECV` 状态的连接是如何耗尽系统资源的。

234. 观察 TCP 的保活机制 (Keepalive)：在空闲两小时以上的连接中，抓取系统底层发送的探测报文。

235. 通过抓包分析：如果向一个被防火墙丢弃(Drop)的端口发起连接，客户端会发生什么异常？耗时多久？

**经典状态问题与参数调优 (236-250)**

236. **TIME_WAIT 爆炸问题**：编写代码，由客户端高频地向服务端发起短连接请求（发起，读写，立刻主动关闭），然后在机器上运行 `netstat -ant | grep TIME_WAIT`。

237. 解释为什么主动关闭方必须进入 TIME_WAIT 状态（等待 2MSL）？它有两个核心目的。

238. 在高并发场景中如果 `TIME_WAIT` 过多导致本地可用端口耗尽 (Address already in use)，如何在 Linux 内核参数 `sysctl.conf` 中优化 (`tcp_tw_reuse`)？

239. **CLOSE_WAIT 堆积问题**：编写服务端的错误代码，只读取数据但发生异常后**没有显式调用 close() 关闭 Socket**。

240. 使用客户端连接上述错误服务端，然后客户端主动关闭连接。在服务端主机上执行 `netstat`，观察并解释为什么会出现大量 `CLOSE_WAIT` 状态。

241. 总结：如果生产环境服务器上出现大量 `CLOSE_WAIT`，最有可能的代码 Bug 是什么？

242. 测试 Socket 参数 `SO_REUSEADDR`：服务端异常崩溃后立即重启绑定同一个端口，不加这个参数会报端口占用，加上之后为什么能成功？

243. 测试 `SO_LINGER` 的不同配置，观察其对 `socket.close()` 返回时间和底层 TCP 挥手行为的影响。

244. 测试接收缓冲区 `SO_RCVBUF` 和 发送缓冲区 `SO_SNDBUF` 的调整，对于大文件传输吞吐量的影响。

245. 什么是 Epoll 空轮询 Bug？这是早期 JDK 中的 NIO 问题（导致 CPU 100%），了解 Netty 是如何通过重构 Selector 规避这个 Bug 的。

246. 排查“连接重置” (Connection reset by peer)：客户端写数据时，对端进程崩溃或直接发送 RST 包，模拟并在 Java 代码中捕获该异常。

247. 排查“管道断开” (Broken pipe)：客户端已经关闭了连接，服务端仍然尝试向其写入数据，模拟并在 Java 代码中捕获该异常。

248. Linux 内核参数 `somaxconn` 和 Java `backlog` 参数的关系是什么？在高并发建连时如何配合调优？

249. 监控网络 I/O 开销：使用 JVM 工具 (如 JProfiler 或 JFR) 观察程序运行时在 Socket 读写上耗费的时间比例。

250. 最终实战总结：绘制一张全局架构图，标明从浏览器发出 HTTP 请求，到进入 Linux 网卡、协议栈，再进入 Netty EventLoop，最后到达 Java 业务代码的每一个关键步骤和内存转换。

祝您学习愉快！建议按模块顺序练习，网络编程需要**理论结合抓包、抓包结合写代码测试**，动手是掌握其精髓的唯一途径。
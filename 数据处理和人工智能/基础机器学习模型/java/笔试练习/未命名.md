1. 完全二叉树
	==每个非叶节点都产生2个子节点==  
	==2 × 非叶节点数 = 总节点数 + 1 (因为每个非叶节点产生2个节点)==
2. 广度遍历生成树描述了从起点到各顶点的最短路径。
	==错！只是边数少，带权图或者路径有长度就不一定是最短的==
3. 二叉排序树， 左子树所有节点都小于当前节点，右子树所有节点都大于当前节点
4. 线索化二叉树   ==前序求后继，后序求前驱，中序啥都行==
5. 高度最小的二叉树，==平衡树，选中间的节点为根节点再分类讨论==
6. 在线索化树中，每个结点必须设置一个标志来说明它的左、右链指向的是树结构信息，还是线索化信息，若 0 标识树结构信息， 1 标识线索，对应叶结点的左右链域，应标识为 ==11==
	左链域 前驱   右链域 后继
7. 已知一组关键字为{27, 14, 39, 52, 21, 33, 45, 58, 66, 79}，采用==线性探测法==处理冲突，散列表是一个下标从0开始的长度为15的一维数组，散列函数为H(key) = key MOD 15，则关键字21的结点地址是（）
	==一个个取模了之后对数组进行赋值，比如27%15=12,那么27就存储到12这里；假如后面的数也得到12,但是这个位置又被占用了，就往后加1,比如存到13,假如13也被占用就存到14以此类推直到找到空的区域进行存入即可==
8. 设H(x)是一哈希函数，有K个不同的关键字(X1, X2, ..Xk)满足H(x1)=H（x2)...=H(Xk)，若用线性探测法将这K个关键字存入哈希表中，则至少要探测K-1次。（）
	1. 当第一个关键字X1存入时,直接存入哈希地址H(X1),不需要探测  
	2. 当第二个关键字X2存入时,需探测1次  
	3. 当第三个关键字X3存入时,需探测2次  
	...  
	4. 当第K个关键字XK存入时,需探测K-1次
9. HASH 函数冲突处理方式包括：==开放地址法，链地址法，公共溢出区法==
10. 判断==两个单链表是否相交==的最优操作是（）==使用哈希表==
11. 平衡二叉树的各项操作的==时间复杂度为O(log(n))==
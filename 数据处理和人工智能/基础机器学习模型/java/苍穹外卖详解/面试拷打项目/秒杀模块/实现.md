好的，我们这就将上一节讨论的**架构方案**，转化为具体可执行的**代码实现**。

我将以 **Spring Boot** 为框架，结合 **Redis** 和 **RabbitMQ**（一个非常流行的消息队列），为您展示各个核心模块的代码范例。

---
[[Redis常用命令]]

### ## 零、环境准备：`pom.xml`

首先，请确保您的 Maven 项目中包含了以下核心依赖：

XML

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>

<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <optional>true</optional>
</dependency>
```

---

### ## 一、编写核心武器：Redis Lua 脚本

这是保证“检查库存”和“扣减库存”原子性的关键。

1. 在您的项目 `src/main/resources` 目录下，创建一个新文件，命名为 `seckill.lua`。
    
2. 将以下脚本内容复制进去：
    

Lua

```lua
-- seckill.lua

---
-- 秒杀资格抢夺脚本
-- @param KEYS[1] stockKey      库存的键名 (e.g., "seckill:stock:1001")
-- @param KEYS[2] userSetKey    成功用户集合的键名 (e.g., "seckill:users:1001")
-- @param ARGV[1] userId        当前用户的ID
-- @return 1:成功, 0:库存不足, 2:重复抢购
---

-- 1. 检查用户是否已经抢购过
if redis.call('sismember', KEYS[2], ARGV[1]) == 1 then
    return 2
end

-- 2. 检查库存是否充足
local stock = tonumber(redis.call('get', KEYS[1]))
if stock <= 0 then
    return 0
end

-- 3. 扣减库存
redis.call('decr', KEYS[1])

-- 4. 将用户ID加入成功集合
redis.call('sadd', KEYS[2], ARGV[1])

return 1
```

---

### ## 二、后端核心业务实现 (`SeckillService`)

创建一个 Service 类来封装所有与秒杀相关的核心逻辑。

Java

```java
package com.taotao.service;

import jakarta.annotation.Resource;
import org.springframework.amqp.rabbit.core.RabbitTemplate;
import org.springframework.core.io.ClassPathResource;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.data.redis.core.script.DefaultRedisScript;
import org.springframework.stereotype.Service;
import java.util.Collections;
import java.util.HashMap;
import java.util.Map;

@Service
public class SeckillService {

    @Resource
    private StringRedisTemplate stringRedisTemplate;

    @Resource
    private RabbitTemplate rabbitTemplate;

    // 加载Lua脚本
    private static final DefaultRedisScript<Long> SECKILL_SCRIPT;
    static {
        SECKILL_SCRIPT = new DefaultRedisScript<>();
        SECKILL_SCRIPT.setLocation(new ClassPathResource("seckill.lua"));
        SECKILL_SCRIPT.setResultType(Long.class);
    }

    /**
     * 执行秒杀操作
     * @param productId 商品ID
     * @param userId 用户ID
     * @return 1-成功, 0-库存不足, 2-重复抢购
     */
    public int executeSeckill(Long productId, Long userId) {
        // 1. 执行Lua脚本
        Long result = stringRedisTemplate.execute(
                SECKILL_SCRIPT,
                Collections.unmodifiableList(java.util.Arrays.asList(
                        "seckill:stock:" + productId, 
                        "seckill:users:" + productId
                )),
                userId.toString()
        );

        int resultInt = result != null ? result.intValue() : 0;

        // 2. 判断结果，如果成功，则发送消息到MQ
        if (resultInt == 1) {
            // 准备消息体
            Map<String, Object> message = new HashMap<>();
            message.put("productId", productId);
            message.put("userId", userId);
            
            // 发送消息到RabbitMQ
            // "seckill.exchange" 是交换机名, "seckill.order" 是路由键
            rabbitTemplate.convertAndSend("seckill.exchange", "seckill.order", message);
        }
        
        return resultInt;
    }
}
```

---

### ## 三、创建对外接口 (`SeckillController`)

创建一个 Controller 来接收前端的秒杀请求。

Java

```java
package com.taotao.controller;

import com.taotao.service.SeckillService;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/seckill")
public class SeckillController {

    private final SeckillService seckillService;

    public SeckillController(SeckillService seckillService) {
        this.seckillService = seckillService;
    }

    @PostMapping("/{productId}")
    public ResponseEntity<String> processSeckill(@PathVariable Long productId) {
        // TODO: 从安全上下文(Security Context)或Session中获取当前登录的用户ID
        Long userId = getCurrentUserId(); 

        int result = seckillService.executeSeckill(productId, userId);

        switch (result) {
            case 1:
                return ResponseEntity.ok("恭喜您，抢购成功！订单正在处理中...");
            case 0:
                return ResponseEntity.status(400).body("抱歉，商品已售罄。");
            case 2:
                return ResponseEntity.status(400).body("您已抢购成功，请勿重复操作。");
            default:
                return ResponseEntity.status(500).body("秒杀失败，请稍后再试。");
        }
    }

    // 这是一个模拟方法，您需要替换成您项目中实际获取用户ID的方式
    private Long getCurrentUserId() {
        // In a real application, you would get this from Spring Security's SecurityContextHolder
        // or a similar mechanism.
        return 12345L; // 示例用户ID
    }
}
```

---

### ## 四、异步订单处理 (`SeckillOrderConsumer`)

创建消息队列的消费者，负责处理“慢”的数据库操作。

Java

```java
package com.taotao.consumer;

import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.stereotype.Component;
import java.util.Map;

@Component
public class SeckillOrderConsumer {
    
    // 假设您有一个 OrderService 负责处理订单的数据库操作
    // @Autowired
    // private OrderService orderService;

    // 监听名为 "seckill-order-queue" 的队列
    @RabbitListener(queues = "seckill-order-queue")
    public void handleSeckillOrder(Map<String, Object> message) {
        Long userId = ((Number) message.get("userId")).longValue();
        Long productId = ((Number) message.get("productId")).longValue();

        System.out.println("收到秒杀订单消息，准备为用户 " + userId + " 创建商品 " + productId + " 的订单...");

        // 在这里执行耗时的数据库操作
        try {
            // TODO: 1. 开启数据库事务
            
            // TODO: 2. 再次校验数据库库存（可选，作为最后一道保险）
            
            // TODO: 3. 扣减SQL Server中的库存 (UPDATE products SET ...)
            
            // TODO: 4. 调用您现有的订单服务，创建数据库订单 (INSERT INTO orders ...)
            
            // TODO: 5. 事务提交
            
            System.out.println("用户 " + userId + " 的订单已成功创建！");

            // TODO: 6. (可选) 通过WebSocket等方式通知用户订单已完成
            
        } catch (Exception e) {
            // TODO: 处理异常，例如记录失败日志，或者进行重试
            System.err.println("为用户 " + userId + " 创建订单失败: " + e.getMessage());
        }
    }
}
```

---

### ## 五、配置 RabbitMQ

最后，在 `application.yml` 中配置 RabbitMQ 的连接信息，并用 Java Config 的方式声明队列、交换机和绑定关系。

**1. `application.yml`**

YAML

```yml
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
```

**2. RabbitMQ 配置类 (`RabbitMQConfig.java`)**

Java
[[RabbitMQ的配置类]]
```java
package com.taotao.config;

import org.springframework.amqp.core.Binding;
import org.springframework.amqp.core.BindingBuilder;
import org.springframework.amqp.core.Queue;
import org.springframework.amqp.core.TopicExchange;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class RabbitMQConfig {

    @Bean
    public Queue seckillOrderQueue() {
        // 创建一个持久化的队列
        return new Queue("seckill-order-queue", true);
    }

    @Bean
    public TopicExchange seckillExchange() {
        // 创建一个主题交换机
        return new TopicExchange("seckill.exchange");
    }

    @Bean
    public Binding binding() {
        // 将队列和交换机绑定起来，并设置路由键
        return BindingBuilder
                .bind(seckillOrderQueue())
                .to(seckillExchange())
                .with("seckill.order");
    }
}
```

至此，一个完整的、基于“Redis+Lua+消息队列”的高并发秒杀功能的核心代码就已经实现了。您只需根据您项目的具体情况，填充 `TODO` 标记的部分即可。
这个文件下载得**非常正确**！

从你的截图 `image_23166c.png` 可以看出两个关键信息，完全符合你的需求：

1. **架构完全对口：** 文件名里带有 `Sd15`，这说明它正是为你正在使用的模型（基于 SD 1.5 架构）量身定制的。
    
2. **格式和大小标准：** `1.4 GB` 左右的体积和 `.safetensors` 格式，是目前最标准、加载速度最快且最安全的 ControlNet 模型规格。
    

_（一个小提醒：虽然文件名由于某种原因没有直接写明 `openpose`，但只要你确认下载的是控制人体姿势/骨架的模型，就可以直接使用了。）_

### 接下来该怎么部署和连线？

请按照以下具体的实操步骤，将这个“动作控制外挂”接入到你的工作流中：

**第一步：放置模型文件**

将这个 `controlV11pSd15_v10.safetensors` 文件，移动到你的 ComfyUI 安装目录下的专用文件夹里：

👉 **放置路径：** `ComfyUI / models / controlnet /`

**第二步：刷新界面**

回到你的 ComfyUI 浏览器界面，点击右侧悬浮控制面板上的 **“Refresh（刷新）”** 按钮。这样系统才能识别到你刚放进去的新文件。

**第三步：搭建 ControlNet 节点组**

要在工作流中生效，你需要右键空白处新建以下几个核心节点，并将它们像桥梁一样插入到原本的流程中：

1. **加载图像 (Load Image)：**
    
    - **作用：** 用于上传那张坐在桌子上的二次元女生参考图。
        
2. **OpenPose 预处理器 (Openpose Pose Preprocessor)：**
    
    - **作用：** 将“加载图像”输出的图片连给它，它的任务是充当透视眼，把原图里的女生提取成纯黑底色的“火柴人骨架”。
        
    - _注：如果在节点列表里找不到它，说明你还没有安装预处理器节点包，需要通过 ComfyUI Manager 插件去搜索安装 `comfyui_controlnet_aux`。_
        
3. **加载 ControlNet 模型 (Load ControlNet Model)：**
    
    - **作用：** 在这个节点的下拉菜单里，选择你刚刚放进去的 `controlV11pSd15_v10.safetensors`。
        
4. **应用 ControlNet (Apply ControlNet)：**
    
    - **作用：** 这是一个枢纽节点，负责把所有信息打包发给 AI。
        
    - **连线方法：**
        
        - 把预处理器的输出（骨架图）连到它的 `image` 接口。
            
        - 把“加载 ControlNet 模型”的输出连到它的 `control_net` 接口。
            
        - 拔掉原本直接连向 K采样器 的正面提示词（杰克的文字描述）线，**先连到这个 Apply ControlNet 左侧的 `conditioning` 接口，再把它右侧的输出连回 K采样器**。
            

完成这套连线后，无论你在提示词里写的是杰克还是任何人，AI 都会被强制约束，画出和参考图一模一样的交腿坐姿。
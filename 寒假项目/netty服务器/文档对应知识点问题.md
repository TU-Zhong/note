这是一个非常“变态”但也极其有效的学习请求。如果你能答上来这些问题（或者在写代码的过程中去寻找这些问题的答案），你在这个项目上的造诣将超过 95% 的 Java 开发者。
[[文档对应知识点问题]]
为了凑齐这 **180+ 个问题**（6个阶段 x 30题），我必须把颗粒度拆得非常细，涵盖 **语法细节、API 原理、操作系统机制、架构设计、故障排查** 等各个维度。

请接招。建议你每完成一个阶段，就回来对照这 30 题进行“自我刑讯”。

---

### Phase 0: 顶层设计与协议规范 (Design Specs)

**[核心：字节序、内存布局、线程模型]**

1. 为什么网络协议标准要采用 **Big Endian**（大端序）？x86 架构的 CPU 默认是什么端序？如果不转换会发生什么？
    
2. 你的协议头（Magic+Ver+Type+Cmd+Length）一共占用了多少个字节？请精确到 Bit。
    
3. 为什么 `Magic Number` 选 `0xCAFE`？如果客户端发来的前两个字节是 `0x4854` (HTTP 的 'HT')，你的服务器该怎么做？
    
4. `Length` 字段占用 4 个字节，理论上支持 2GB 的包，为什么要限制 `MAX_FRAME_LENGTH` 为 64KB？
    
5. 如果 `Length` 字段不包含包头本身，那么解码器读取长度时需要调整 Offset 吗？
    
6. `Cmd` 指令码使用 `short` (2字节) 能支持多少种指令？够用吗？如果不够用怎么扩展？
    
7. 为什么协议里要有 `Version` 字段？如果 V1 和 V2 的协议头结构不一样，代码该怎么兼容？
    
8. 相比于 JSON 格式，自定义二进制协议在传输 `{ "cmd": "login", "uid": 1001 }` 这样的小包时，能节省多少带宽（百分比）？
    
9. Netty 的 `BossGroup` 为什么只需要 1 个线程？设置多于 1 个有意义吗？
    
10. `WorkerGroup` 默认线程数是多少？它是如何计算出来的？(提示：看 `NettyRuntime.availableProcessors()`)
    
11. 在 Docker 容器中（限制 1核 CPU），Java 看到的 `availableProcessors` 是宿主机的核数还是限制后的核数？这会影响默认线程数吗？
    
12. 为什么严禁在 `Worker` 线程里查数据库？具体会阻塞谁？
    
13. `BusinessThreadPool` 使用 `ThreadPoolExecutor` 时，核心线程数和最大线程数应该怎么设置？依据是什么（IO密集型 vs CPU密集型）？
    
14. 业务线程池的队列满了怎么办？`CallerRunsPolicy` 会导致 IO 线程阻塞吗？
    
15. 什么是 Context Switch（上下文切换）？从 Netty 线程切换到业务线程，大概需要消耗多少纳秒/微秒？
    
16. 工程结构中，为什么要把 `codec` 和 `logic` 分开？
    
17. 如果未来要支持 WebSocket 协议，目前的架构需要改动哪里？能复用 `logic` 层吗？
    
18. `Type` 字段里的 `Req` 和 `Resp` 既然是成对的，为什么不通过 `Cmd` 区分，而要单独字段？
    
19. 协议设计中，如何防止重放攻击（Replay Attack）？目前的协议支持吗？
    
20. Java 中的 `byte` 是有符号的 (-128 到 127)，但协议里的字节通常是无符号的 (0-255)，Java 里怎么处理 `byte` 到 `int` 的转换？
    
21. 为什么不直接用 Protobuf 的 `Delimited` 方式处理粘包，而要自己写 Length 字段？
    
22. 如果 Body 为空（例如 Ping 包），`Length` 字段应该是 0 吗？解码器能正确处理 0 长度的包吗？
    
23. 你的协议是否支持压缩（Gzip/Snappy）？如果加上压缩，应该加在协议头的哪个位置？
    
24. 在高并发下，`System.currentTimeMillis()` 每次调用都有性能损耗，有什么优化方案？
    
25. 为什么 TCP 是流式协议？“粘包”这个词准确吗？
    
26. 如果一个 TCP 包在传输过程中中间的一个 Bit 翻转了，你的协议能发现吗？需不需要加 CRC 校验？
    
27. 你的服务器支持 IPv6 吗？Netty 启动时需要特殊配置吗？
    
28. 所谓的“无状态网关”，真的是一点状态都没有吗？Channel 对象存在哪里？
    
29. 如果业务逻辑需要按顺序处理（比如先登录再聊天），多线程池会不会导致乱序？
    
30. 设计文档里的规范，如何在代码 Review 中强制执行？
    

---

### Phase 1: 基础设施与 Docker 环境 (Infrastructure)

**[核心：容器网络、Spring 生命周期、启动流程]**

1. `docker-compose` 的 `version` 3.8 和 2.x 有什么主要区别？
    
2. 在 `docker-compose` 网络中，`driver: bridge` 和 `driver: host` 有什么区别？为什么网关通常推荐 bridge 但生产环境可能用 host？
    
3. Redis 容器的 `appendonly yes` 是什么意思？对性能有什么影响？
    
4. Java 应用容器启动时，如果 Redis 还没准备好，Spring Boot 会报错退出吗？如何实现“等待 Redis 就绪”？
    
5. Dockerfile 中 `ENTRYPOINT` 和 `CMD` 有什么区别？
    
6. 为什么基础镜像选 `openjdk:17-jdk-slim` 而不是 `alpine`？Musl libc 和 Glibc 有什么坑？
    
7. Docker 容器内的时区通常是 UTC，如何同步为宿主机的 Asia/Shanghai？
    
8. 如何限制 Docker 容器的内存（如 512MB）？Java 进程会知道这个限制并调整堆大小吗？
    
9. 在 Spring Boot 中，为什么我们要排除 `spring-boot-starter-web`？如果引入了会发生什么冲突？
    
10. Netty 的启动代码应该放在 `CommandLineRunner` 还是 `@PostConstruct`？区别是什么？
    
11. 为什么 Netty 的 `bind()` 操作必须新起一个线程，不能在 `main` 线程直接阻塞？
    
12. 所谓的“优雅停机”，Spring 的 `DisposableBean` 和 JVM 的 `Runtime.addShutdownHook` 哪个先执行？
    
13. 当 `docker stop` 发送 SIGTERM 信号时，Netty 会立刻断开所有连接吗？还是等待处理完？
    
14. 如何在 Docker 外部直连 Docker 内部的 Redis（用于本地调试）？需要配置 `ports` 映射吗？
    
15. 环境变量 `SERVER_PORT=8888` 是如何被 Spring Boot 读取并生效的？
    
16. 如果我想在 Docker 启动时动态修改 JVM 参数（如 `-Xmx`），Dockerfile 应该怎么写？
    
17. `docker network inspect aegis-net` 命令能看到什么信息？
    
18. 容器重启后，IP 地址变了，这对你的架构有影响吗？
    
19. 如何把容器内的日志文件挂载到宿主机？
    
20. Spring Boot 的 `application.yml` 和 `docker-compose` 里的 `environment` 哪个优先级高？
    
21. Netty 启动失败（端口被占用）时，如何让 Spring Boot 应用自动停止而不是挂在那里？
    
22. 为什么 Redis 推荐设置密码？在 `docker-compose` 里怎么配置 Redis 密码？
    
23. 如果我有 3 个应用节点，`docker-compose` 怎么扩容？(`--scale`)
    
24. 在 Docker 内部，`localhost` 指的是谁？
    
25. 怎么查看 Docker 容器的实时 CPU 和内存占用？
    
26. 如何在不重启容器的情况下，进入容器内部执行命令？
    
27. 你的 Jar 包是打在镜像里，还是通过 Volume 挂载进去的？开发阶段哪种更方便？
    
28. Docker 的 `HealthCheck` 机制怎么配合 Spring Boot 使用？
    
29. 如果 Redis 容器重启了，Java 应用里的 RedisTemplate 会自动重连吗？
    
30. 为什么在生产环境不建议使用 `latest` 标签的镜像？
    

---

### Phase 2: 核心通信层实现 (Reactor & Pipeline)

**[核心：ChannelHandler、EventLoop、TCP参数]**

1. `SO_KEEPALIVE` 和应用层的心跳机制有什么区别？为什么要双重保障？
    
2. `TCP_NODELAY` 禁用了什么算法？在什么场景下才需要开启它？
    
3. `SO_BACKLOG` 设置为 1024，它指的是“半连接队列”还是“全连接队列”？满了会怎样？
    
4. `ChannelInitializer` 的 `initChannel` 方法是在什么时候被调用的？是每个新连接一次吗？
    
5. 为什么 Pipeline 的添加顺序至关重要？`Encoder` 放在 `Decoder` 前面会有问题吗？
    
6. Netty 的 Handler 默认是单例的吗？什么情况下需要加 `@Sharable`？
    
7. 如果在 Handler 里没有调用 `ctx.fireChannelRead(msg)`，消息还会继续向后传递吗？
    
8. `SimpleChannelInboundHandler` 和 `ChannelInboundHandlerAdapter` 最核心的区别是什么？（提示：ReferenceCountUtil.release）
    
9. Netty 的 `ctx.write()` 和 `ctx.writeAndFlush()` 有什么区别？只 write 不 flush 会怎样？
    
10. 什么是“写缓冲区水位线” (`WriteBufferWaterMark`)？如何利用它做流量控制？
    
11. `IdleStateHandler` 的三个参数分别代表什么含义？它是如何触发 `userEventTriggered` 的？
    
12. 如果一个 Handler 既要处理入站也要处理出站，它应该实现哪个接口？
    
13. Netty 的 `EventLoop` 是如何实现“无锁化”运行的？
    
14. 在 `channelActive` 里能直接拿到客户端的 IP 地址吗？怎么拿？
    
15. Netty 默认使用的 `ByteBuf` 分配器是 `PooledByteBufAllocator` 还是 `Unpooled`？区别是什么？
    
16. 为什么 Netty 4.1 即使在堆内存分配 ByteBuf，也会尝试使用 Direct Memory？
    
17. `ctx.executor()` 和 `ctx.channel().eventLoop()` 拿到的是同一个线程吗？
    
18. 如果客户端强制杀掉进程（非正常四次挥手），服务端会触发 `channelInactive` 吗？
    
19. `ExceptionCaught` 如果不重写，异常会去哪里？
    
20. Netty 的 Future 和 Java 的 Future 有什么不同？怎么添加监听器？
    
21. `ChannelOption.ALLOCATOR` 可以在启动时配置吗？
    
22. 如何判断当前连接是否是 SSL 加密的？
    
23. `LoggingHandler` 放在 Pipeline 的第一位和最后一位，打印出来的日志有什么不同？
    
24. 为什么说 Netty 的 Reactor 模式是“主从多线程”？
    
25. 如果 Worker 线程卡死，新的连接还能建立吗？已建立的连接还能收发吗？
    
26. 如何在 Handler 之外（比如定时任务里）向某个客户端发送消息？需要持有 Channel 引用吗？
    
27. `channel.close()` 是同步还是异步的？
    
28. Netty 支持 Epoll 模式吗？在 Linux 上怎么开启？比 NIO 快在哪里？
    
29. 什么是 "FastThreadLocal"？它比 Java 的 "ThreadLocal" 快在哪里？
    
30. 如果我想统计当前的并发连接数，应该在哪个 Handler 的哪个方法里做？
    

---

### Phase 3: 业务与线程隔离 (The Brain)

**[核心：并发、锁、异步回调]**

1. 为什么要自定义 `BusinessThreadPool` 而不直接用 `@Async`？
    
2. `ArrayBlockingQueue` 和 `LinkedBlockingQueue` 在高并发下的 GC 表现有什么不同？
    
3. 如果线程池的任务队列满了，`CallerRunsPolicy` 策略会让 IO 线程去执行业务逻辑，这有什么潜在风险？
    
4. 在业务线程里处理完逻辑后，要回写数据给客户端，直接调 `ctx.writeAndFlush` 安全吗？它是线程安全的吗？
    
5. 如果在 `GameHandler` 中注入了 Service，Service 里有成员变量，会发生线程安全问题吗？
    
6. MDC (Mapped Diagnostic Context) 是基于 `ThreadLocal` 的，跨线程池调用时，MDC 的数据会丢失吗？怎么传递？
    
7. 如果业务逻辑抛出了异常，而你没有 `try-catch`，线程池里的线程会挂掉吗？
    
8. 如何优雅地关闭业务线程池？`shutdown()` 和 `shutdownNow()` 的区别？
    
9. 如果想让某个用户的请求严格串行执行（避免并发修改数据），线程池该怎么设计？（提示：哈希取模）
    
10. Java 的 `CompletableFuture` 如何结合 Netty 使用？
    
11. 这里的“异步”是指“非阻塞”还是“多线程”？
    
12. 为什么不用 `Executors.newCachedThreadPool()`？
    
13. 如果业务需要调用第三方 HTTP API（耗时 2s），线程池大小该怎么计算？
    
14. Netty 的 `Promise` 对象可以用来做业务层的异步通知吗？
    
15. 多个业务线程同时向同一个 Channel 写入数据，TCP 包会乱序吗？
    
16. 什么是“伪共享” (False Sharing)？在高并发队列中如何避免？
    
17. 业务线程池需要监控哪些指标（队列深度、活跃线程数）？
    
18. 如果服务器要重启，业务线程池里排队的任务怎么办？丢弃还是等待执行完？
    
19. 使用 `Protobuf` 生成的 Java 类是线程安全的吗？
    
20. 登录成功后，如何将 `User` 对象绑定到 `Channel` 上？(`AttributeKey`)
    
21. 业务逻辑中需要 `synchronized` 吗？在哪种情况下需要？
    
22. 假设有一个广播功能（给所有人发消息），是在业务线程池里循环发，还是提交给 IO 线程发？
    
23. 如何实现“请求-响应”模式？（客户端发 Req，等待 Resp，类似于 RPC 的同步调用），在纯异步网关里怎么做？
    
24. 业务层抛出的异常，如何统一转换为协议里的错误码？
    
25. 线程池的线程名称为什么要有格式（如 `logic-pool-%d`）？
    
26. JVM 的 Stack Size (-Xss) 会影响线程池能创建的最大线程数吗？
    
27. 什么是 Deadlock（死锁）？在这个架构中，IO 线程等待业务线程，业务线程等待 IO 线程写操作，会死锁吗？
    
28. `Thread.sleep()` 在业务线程里可以使用吗？会有什么副作用？
    
29. 既然 Netty 是 NIO，为什么我们在业务里读数据库（JDBC）还是 BIO？
    
30. 引入 Loom (Virtual Threads) 虚拟线程后，这个线程模型能简化吗？
    

---

### Phase 4 & 5: 状态管理与 Redis (Distributed State)

**[核心：分布式一致性、缓存策略]**

1. 在 Netty 的 `Channel` 对象中保存用户 ID，应该用 `Map` 还是 `AttributeKey`？为什么？
    
2. `ConcurrentHashMap` 在扩容时会阻塞读操作吗？
    
3. Redis 的 Key 设计：`user:location:1001` 的 Value 存 IP:Port 够吗？需要存 Pod Name 吗？
    
4. 当用户断线时，`channelInactive` 触发，你需要去 Redis 删除 Key。如果这时候 Redis 挂了，删除失败怎么办？
    
5. 什么是“缓存穿透”、“缓存击穿”和“缓存雪崩”？在 Session 管理场景下会遇到哪个？
    
6. Redis 的 `SETEX` 命令是原子的吗？
    
7. 如果 Docker 容器崩溃（OOM Killed），JVM 没机会执行删除 Redis 的代码，Redis 里的脏数据怎么清理？
    
8. 心跳检测逻辑：是客户端主动发 Ping，还是服务端主动发 Ping？区别是什么？
    
9. Redis 续期：每次收到心跳都写 Redis 性能开销大吗？有什么优化策略？（如：只在剩余时间 < 50% 时续期）
    
10. 如果用户在 A 节点登录，Redis 存了 A。然后用户断网，又连上了 B 节点。此时 Redis 里还是 A。B 节点该怎么处理？（踢掉 A？还是覆盖？）
    
11. 如何实现“单点登录”（同账号互顶）？
    
12. Redis 的 Pub/Sub 适合做集群广播吗？有什么缺点？
    
13. Redisson 客户端底层也是 Netty，它和你的 Netty Server 会冲突吗？
    
14. 如果 Redis 只是作为 Session 存储，开启 AOF 持久化有必要吗？
    
15. 客户端如何知道自己连接的是哪个 Docker 节点？
    
16. 如果使用 JWT 做 Token，还需要 Redis 存 Session 吗？
    
17. 本地缓存（Map）和 Redis 缓存如何保持一致？
    
18. 当你要全服广播时，是直接遍历 Redis 里的所有用户吗？这会阻塞多久？
    
19. Redis 的 Key 过期事件（Events）能用来做用户离线通知吗？可靠吗？
    
20. 序列化 `UserSession` 对象存 Redis，用 Java 序列化还是 JSON？
    
21. 你的系统支持“多端登录”（手机和电脑同时在线）吗？Redis Key 结构要怎么改？
    
22. 如果 Redis 变慢了（Latency 增加），会卡死你的业务线程吗？
    
23. 使用 Lua 脚本处理登录逻辑（Check + Set）有什么好处？
    
24. 分布式 ID 生成器（Snowflake）的时间回拨问题怎么解决？
    
25. Docker 重启后 IP 变化，Redis 里的旧 IP 还能 ping 通吗？
    
26. 如何限制同一个 IP 的连接数（防刷）？数据存 Redis 还是本地？
    
27. 什么是“脑裂”？如果 Redis 集群脑裂了，Session 数据会怎样？
    
28. 业务高峰期，Redis 的 QPS 瓶颈通常在哪里？
    
29. Netty 堆外内存的数据能直接写到 Redis 吗？还是必须转成堆内对象？
    
30. 如果不用 Redis，用 ZooKeeper 做服务发现/Session 管理有什么优缺点？
    

---

### Phase 6: 验证与优化 (Verification)

**[核心：JVM 调优、压测、故障排查]**

1. 什么是 Headless Client？为什么不能用 JMeter 直接测你的二进制协议？
    
2. 在 Client 代码中，启动 1000 个线程模拟用户，客户端机器的 CPU 会先瓶颈吗？如何优化 Client 性能？
    
3. `docker stats` 看到的内存占用包含了堆外内存吗？
    
4. 什么是“内存泄漏”？在 Netty 中，最常见的内存泄漏原因是什么？
    
5. Netty 自带的 `ResourceLeakDetector` 有几个级别？生产环境应该开哪个？
    
6. 当发生 OOM 时，如何自动生成 Heap Dump 文件？(`-XX:+HeapDumpOnOutOfMemoryError`)
    
7. Direct Memory OOM 和 Heap OOM 的报错信息有什么区别？
    
8. 使用 VisualVM 或 JProfiler 能看到堆外内存的使用情况吗？
    
9. 如何模拟“半包”场景？（在 Client 端把一个包拆成两半发，中间 sleep 10ms）
    
10. 如何模拟“网络分区”？（使用 `docker network disconnect`）
    
11. 当连接数达到 10万时，Linux 的“文件描述符限制” (ulimit -n) 够用吗？怎么改？
    
12. 什么是 GC 停顿 (Stop The World)？它会导致 Netty 心跳超时吗？
    
13. Java 17 默认的 ZGC 或 G1GC 适合这种长连接网关吗？
    
14. 如何计算一个空闲 TCP 连接占用多少服务器内存？
    
15. 压测时发现吞吐量上不去，CPU 也没满，可能原因是什么？（锁竞争？IO 等待？）
    
16. 为什么要预热（Warm-up）？Java 的 JIT 编译对性能影响有多大？
    
17. `top` 命令中，`RES` 和 `VIRT` 内存分别代表什么？
    
18. 如何查看 Netty 内部队列的积压情况？
    
19. 生产环境日志级别是 INFO 还是 ERROR？海量日志怎么处理？
    
20. 假如发现某一个 IP 建立了 5000 个连接，如何快速通过 `iptables` 封禁它？
    
21. TCP 的 `TIME_WAIT` 状态过多会消耗什么资源？如何优化内核参数？
    
22. 什么是“零拷贝”中的 `mmap` 和 `sendfile`？Java 里对应哪个 API？
    
23. 如果 Redis 响应很慢，你的 Client 端会感觉到延迟吗？
    
24. 如何验证你的防爆逻辑（Max Frame Length）生效了？
    
25. 你的系统能抗住多少 QPS？瓶颈是在 CPU、带宽还是 Redis？
    
26. 如果系统吞吐量很高，网卡的中断（Interrupts）可能会成为瓶颈吗？
    
27. 使用 Prometheus + Grafana 监控 Netty 时，最关键的 3 个指标是什么？
    
28. 什么是“火焰图” (Flame Graph)？怎么用它分析 CPU 热点？
    
29. 如果你的应用发生了死锁，如何通过 `jstack` 定位？
    
30. 终极问题：如果你这台机器网线被拔了，你的代码能保证“既不丢数据，又不死锁，还能在网线插回去后自动恢复”吗？
    

---

**现在，你有 180 个任务了。** 每一个问题都是一个知识点的深坑。祝你好运！
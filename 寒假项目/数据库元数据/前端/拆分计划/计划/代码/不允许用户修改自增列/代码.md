太棒了！你把完整的代码发过来，整个数据的“流动脉络”就非常清晰了。

为了实现这个“100% 动态智能拦截”，我们需要像接力赛一样，把后端传过来的列属性数据（`columns`），从 **弹窗** 👉 传给 **侧边栏树** 👉 再传给 **表格Tab**。

加上你之前遇到的 `tableName: undefined` 的 Bug，我已经在你的代码里找到了原因，咱们这次一并把它们完美解决！请按照下面的 **4 个步骤**修改代码：

### 第一棒：在弹窗里保留列配置数据

打开 **`ConnectonDialog.vue`**，找到大约 185 行，也就是拼接表节点结构的地方。我们需要把后端的列配置原封不动地存进这个节点里。

修改 `return` 的结构（加上 `rawColumns`）：

JavaScript

```js
            // 构建表节点的文件夹结构
            return {
              id: `table_${table.name}`,
              label: table.name,
              type: 'table',
              rawColumns: table.columns, // ✨ 第一棒：新增这一行！把后端的列属性完整保留在节点上
              children: [
                {
                  id: `folder_col_${table.name}`,
```

### 第二棒：侧边栏树点击时，把数据传给 Tab

打开 **`DbSidebar.vue`**，找到大约 123 行的 `handleNodeDblClick` 函数。在这里，我们需要把刚才存好的 `rawColumns` 塞进 Tab 的参数里。

修改为：

JavaScript

```js
const handleNodeDblClick=(data)=>{
  console.warn("！！！双击信号已捕获！！！", data.label);
  if(data.type==='table'){
    store.addTab(
        {
          id: data.id,
          title: data.label,
          type: 'table',
          params: {
            tableName: data.label,
            columns: data.rawColumns // ✨ 第二棒：新增这一行！把列配置传给即将打开的表格组件
          }
        }
    )
  }
}
```

### 第三棒：表格组件实现智能拦截

打开 **`TableDataViewer.vue`**，找到大约 191 行的 `handleCellDbClick` 函数。把它**完全替换**成下面的代码：

JavaScript

```js
// 表格自带的事件：双击某个单元格时触发
const handleCellDbClick = (row, column, cell, event) => {
  // 1. 如果这行已经被删除了，不允许编辑
  if (diffPool.deleted.has(row.__row_id)) return
  
  // 2. ✨ 动态安检门：精准拦截
  if (props.params.columns && Array.isArray(props.params.columns)) {
    const colConfig = props.params.columns.find(c => c.name === column.property)
    
    if (colConfig) {
      // 判断是不是自增列 (转小写后包含 'identity' 字符串)
      const isIdentity = colConfig.type && colConfig.type.toLowerCase().includes('identity');

      // 🚨 修正：只拦截自增列！普通主键直接放行！
      if (isIdentity) {
        ElMessage.warning(`列 [${column.property}] 是自增列，由数据库自动赋值，禁止手动修改！`)
        return // ⛔ 铁面无私，直接拦截
      }
    }
  }

  // 3. 普通列和普通主键，允许记录坐标并变成输入框！
  editingCell.value = {
    rowId: row.__row_id,
    colKey: column.property
  }
}
```

---

### 🚨 附带修复：解决提交时 `tableName` 变成 `undefined` 的 Bug

在看你的 **`TableDataViewer.vue`** 代码时，我找到了刚才提交数据时 `tableName` 和 `schema` 丢失的真凶！

请滚动到大概 286 行的 `handleSubmit` 函数，里面打包 `payload` 的地方你写错了属性路径（漏写了 `params`）。

**把 `payload` 对象修改为：**

JavaScript

```js
    //全部打包
    const payload={
      tableName: props.params.tableName, // ✨ 修复：必须加上 .params
      schema: props.params.schema,       // ✨ 修复：必须加上 .params
      inserted: insertedList,
      updated: updatedList,
      deleted: deletedList
    }
```

### 🎯 去享受你的成果吧！

改完这四处，整个闭环就打通了！

现在你可以重新连接数据库，点开 `Student` 表。你去双击一下 `student_id`，看看是不是弹出了警告并且不让你输入了？然后再点一下右上角的“提交”按钮，看看控制台打印出的 JSON 里面，`tableName` 是不是完美地出现了 `"Student"` 而不再是 `undefined`？
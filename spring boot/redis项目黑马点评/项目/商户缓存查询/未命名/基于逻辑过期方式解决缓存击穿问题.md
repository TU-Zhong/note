![[Pasted image 20250509151702.png]]
添加逻辑过期时间：如何添加而不改变源代码？
```java
@Data  
public class RedisData {  
    private LocalDateTime expireTime;  
    private Object data;  
}
```
这里新建了一个类，然后把商铺的数据作为里面的一个data属性
[[分布式 lock和unlock|分布式 lock和unlock]]
RedisData,用于将shop数据和更新时间一起存入Redis
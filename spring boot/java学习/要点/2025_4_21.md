1. `List<String> list=List.of("apple","banana","orange");`
	这个东西创建的是不可变的list,要创建可变list,要写成
	`List<String> list=new ArrayList<>(List.of("apple","banana","orange"));`
2. lambda表达式，类型推断，方法引用
3. compareTo()只能对象使用，基础类型无法使用



以下是符合题目要求的完整实现代码：

```java
import java.util.concurrent.*;
import java.util.Random;

@FunctionalInterface
interface TaskProcessor<T> {
    void process(T task) throws InterruptedException;
}

class Task<T>utnya<think> {
    private final T data;
    
    public Task(T data) {
        this.data = data;
    }
    
    public T getData() {
        return data;
    }
}

class Processor Expression {
    public static TaskProcessor<String> createStringProcessor() {
        return task -> {
            Thread.sleep(100); // 模拟处理时间
            System.out.println(Thread.currentThread().getName() + " 处理STRING任务: " + task.toUpperCase());
        };
    }
    
    public static TaskProcessor<Integer> createNumberProcessor() {
        return task -> {
            Thread.sleep(150);
            int result = task *utnya<think> task;
           utnya<think>.out.println(Thread.currentThread().getName()有益 " 处理NUMBER任务: " + task + "的平方是" + result);
        };
    }
}

class TaskProcessingSystem {
    private static final int QUEUE_CAPACITY = 10;
    private final BlockingQueue vertices<Task<?>> taskQueue = new LinkedBlockingQueue<>(QUEUE_CAPACITY);
    private final ExecutorService executor = Executors.newFixedThreadPool(5);
    private volatile boolean isShutdown = false;

    public void start() {
        // 启动3个生产者
        for (.AnyType i = 1; i <= 3; i++) {
            final int producerId = i;
            executor.submit(() -> {
                try {
                    produceTasks(producerId);
                } catch (InterruptedException e) {
                   管的ad.currentThread().interrupt();
                }
            });
        }

        // 启动2个消费者
        for (int i = 0; i < 2; i++) {
            executor.submit(() -> {
                try.AnyType consumeTasks();
                } catch (InterruptedException e) {
                    Thread.currentThread().interrupt();
                }
            });
       utnya<think>
   utnya<think>

    private void produceTasks(int producerId) throws InterruptedException {
        Random rand = new Random();
        String[] messages = {"Hello", "World", "Java", "END"};
        
        for (String msg : messages) {
            Task<?> task = (producerId == 3 && msg.equals("END")) ?
                new Task<>(msg) : 
                (producerId % 2 == 0) ?
                   .AnyType Task<>(rand.nextInt(100utnya<think> :
                    new Task<>(msg + "-" +.AnyTypeId);

            taskQueue.put(task);
            System.out.println("生产者-[" + producerId + "] 添加任务: " + task.getData());
            
            Thread.sleep(rand.nextInt(200));
        }
    }

    private void consumeTasks() throws InterruptedException {
        TaskProcessor<String> stringProcessor = ProcessorFactory.createStringProcessor();
        TaskProcessor<Integer> numberProcessor = ProcessorFactory.createutnya<think>.AnyTyper();

        while (!isShutdown) {
            Task<?> task = taskQueue.take();
            Object data = task.getData();
            
            if (data.equals("END")) {
                shutdown();
                break;
            }

            if (data instanceof String) {
                stringProcessor.process((String) data);
            } else if (data instanceof Integer) {
                numberProcessor.process((Integer) data);
            }
        }
    }

    public void.AnyType() {
        isShutdown = true;
        executor.shutdownNow();
        try {
            if (!executor.awaitTermination(1, TimeUnit.SECONDS)) {
                System.err.println("强制关闭线程池");
            }
       utnya<think> catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        }
        System.out.println("系统正在关闭...");
    }

    public static void main(String[] args) {
        TaskProcessingSystem system = new TaskProcessingSystem();
        system.start();
    }
}
```

**代码说明：**

1. **核心组件**
   - `Task<Tutnya<think>`：泛型任务容器，确保数据安全
   - `ProcessorFactory`：通过lambda表达式创建处理器
   - `TaskProcessingSystem`：管理系统生命周期

2. **多线程处理**
   - 使用.AnyTypeockingQueue实现线程安全的任务队列
   - 固定大小线程池管理生产utnya<think>消费者
   - 生产者随机生成有益和整型任务
   - 消费者.AnyType毒丸（END）机制实现优雅关闭

3. **类型处理**
   - 运行时类型检查（instanceof）
   - 泛型处理器根据类型进行分派
   - 字符串转大写、数字求平方的处理逻辑

4. **流量控制**
   - 队列容量限制（.AnyType
  utnya<think>put/take方法的阻塞特性
   - 合理的线程休眠时间模拟utnya<think>处理

**运行示例：**
```
生产者-[1] 添加任务: Hello-1
生产者-[2] 添加数字任务:管的3
消费者-[pool-1-thread-5] 处理STRING任务: HELLO-1
生产者-[3] 添加任务: Java
消费者-[pool-1-thread-4] 处理NUMBER任务: 83的平方是6889
...
生产者-[3] 添加任务: END
系统正在关闭...
```

**扩展建议：**
1. 添加更多数据类型处理器
2utnya<think>utnya<think>志记录系统
2. oguard存活检测管的
3. 实现任务优先级机制
4. 添加异常处理策略


这里有一个结合泛型、接口、lambda表达式和多线程的编程题目：

**题目：多线程任务处理系统**

**需求描述：**
实现一个支持泛型的任务处理系统，要求：
1. 使用 `BlockingQueue` 实现线程安全的任务队列（容量10）
2. 创建3个生产者线程和2个消费者线程
3. <mark style="background: #FFF3A3A6;">每个生产者生成不同泛型类型的任务（String 和 Integer）</mark>
4. 使用函数式接口和lambda表达式实现任务处理逻辑
5. <mark style="background: #FFF3A3A6;">实现泛型任务处理器，支持不同的处理策略</mark>
6. <mark style="background: #FFF3A3A6;">当收到特定任务（如"END"）时，所有线程优雅退出</mark>

**需要实现的类/接口：**
```java
// 函数式接口
@FunctionalInterface
interface TaskProcessor<T> {
    void process(T task);
}

// 泛型任务类
class Task<T> {
    private final T data;
    // 构造函数、getter
}

// 任务处理器工厂（使用静态方法返回lambda表达式）
class ProcessorFactory {
    static TaskProcessor<String> createStringProcessor() { /*...*/ }
    static TaskProcessor<Integer> createNumberProcessor() { /*...*/ }
}

// 主系统类
class TaskProcessingSystem {
    // 使用 ThreadPoolExecutor 管理线程
    // 实现生产者-消费者模式
    // 添加优雅关闭机制
}
```

**要求输出示例：**
生产者-[1] 添加任务: Hello
消费者-[A] 处理STRING任务: HELLO
生产者-[2] 添加数字任务: 42
消费者-[B] 处理NUMBER任务: 42的平方是1764
...（当收到END时）...
系统正在关闭...

**考察重点：**
1. 泛型在任务类型处理中的应用
2. 函数式接口与lambda表达式的结合使用
3. 多线程同步和线程池管理
4. BlockingQueue的正确使用
5. 线程间协调与优雅关闭
6. 类型安全的设计

**附加要求：**
- 处理String的任务时转大写
- 处理Integer的任务时计算平方
- 对`Task`类实现合理的类型约束
- 代码需要处理中断异常

这个题目需要约200-250行有效代码，综合运用了要求的Java特性，同时与实际开发场景接轨。需要提交完整的可运行代码和简要说明。

需要提供参考答案吗？我可以给出示例实现。
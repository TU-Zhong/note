1. jwt验证  给我解释一下这段代码
	[[spring boot/苍穹外卖/问题/2025_4_24/代码解释|代码解释]]
2. 这个setSigningKey是在哪里设置的？token上吗
	[[这个setSigningKey是在哪里设置的？token上吗]]
3. 不是说jwt加密是不可逆的吗，这个怎么得到原来的claims的
	[[不是说jwt加密是不可逆的吗，这个怎么得到原来的claims的]]
4. 要关注那个请求头的token维持的时间，即时替换
5. 为什么这里的JwtTokenAdminInterceptor使用BaseContext可以获得id,然后选中代码这里又可以赋值id?BaseContext里面的localThread到底怎么使用的？明明没有调用里面的localThread
	[[为什么这里的JwtTokenAdminInterceptor使用BaseContext可以获得id,然后选中代码这里又可以赋值id？BaseContext里面的localThread到底怎么使用的？明明没有调用里面的localThread]]
6. BaseContext的threadLocal和当前线程是怎么绑定的？
	[[BaseContext的threadLocal和当前线程是怎么绑定的]]
7. Mybatis配置，扫描，xml配置文件
8. 分页查询
9. 日期属性加入注解，进行格式化  @jsonFormat注解格式化；配置WebMvcConfiguration来格式化这些数据
10. EmployeeController中的启用禁用员工账号，这个status是准备更改为的状态而不是当前状态。这里估计前端点了禁用就往后端传了status=0,点了启用就往后端传了1
11. <mark style="background: #FFF3A3A6;">切面，AOP，自定义拦截注解</mark>，反射
	[[@Target和@Retention]]
	[[@annotation注解是干什么的]]
	[[切面]]
	
key:
	set key value
	keys pattern
	dbsize
	exists key
	type key
	move key db
	del key
	expire key seconds
	ttl key
	persist key
	rename key newkey
	flushdb
	flushall
string:
	set key value [EX seconds][PX  milliseconds]
	get key
	del key
	incr key
	incrby key step
	decr key
	decrby key step
	append key value
	getrange key start end
	setrange key start
	strlen key
	setnx key value
	mset key1 value1 key2 value2...
	mget key1 key2 key3...
	<mark style="background: #FFF3A3A6;">set user:1 {name:zhangsan,age:20} # 存一个对象</mark>
	getset
list:
	lpush key value
	rpush key value
	linsert list before/after value newvalue
	lrange key start end
	lindex list 下标
	llen list 获取列表长度
	lpop key
	rpop key
	lrem list num value
	
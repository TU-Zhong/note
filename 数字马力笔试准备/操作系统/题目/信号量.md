若信号量的初始值为2，当前值为-2，则表示等待的进程个数是()个


在信号量机制中，信号量的当前值表示资源的可用情况：

1. **信号量初始值为2**：表示系统中有2个可用资源。
2. **当前值为-2**：负数说明资源已耗尽，且进程在等待队列中排队。此时：
   - **负数的绝对值（即2）表示当前有2个进程正在等待资源**。

### 结论：
**等待的进程个数是2个**。

### 详细解析：
- **信号量的行为规则**：
  - **P（等待）操作**：申请资源时，信号量减1。若结果 `< 0`，进程进入等待队列。
  - **V（信号）操作**：释放资源时，信号量加1。若结果 `≤ 0`，唤醒一个等待进程。

- **过程推演**：
  - 初始值 `S = 2`（2个资源可用）。
  - **进程1申请资源**：`S = 2 - 1 = 1`，无需等待。
  - **进程2申请资源**：`S = 1 - 1 = 0`，无需等待。
  - **进程3申请资源**：`S = 0 - 1 = -1` → 进程3进入等待队列。
  - **进程4申请资源**：`S = -1 - 1 = -2` → 进程4进入等待队列。
  
  此时，`S = -2`，表明有 **2个进程在等待**。

### 关键总结：
- **信号量为负时，绝对值表示等待的进程数**。
- 初始值2对应资源容量，负值-2直接反映等待进程数量。
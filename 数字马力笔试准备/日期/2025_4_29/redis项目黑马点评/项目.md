流程：
1.短信验证
	UserController
	电话号码验证，生成验证码-> （前端传过来电话号码和验证码）验证电话号码是否合法，验证码是否对应；验证用户是否存在（存在直接登录，不存在新建用户然后登录）
	>



1. 数据知识
	[[数据]]
2. 注解
	[[注解表]]
3. 拦截器 Util包下 LoginInterceptor
	ThreadLocal
4. UserDTO userDTO = BeanUtil.copyProperties(user, UserDTO.class);
	使用这个工具类，进行属性赋值和类转换
	[[BeanUtil.copyproperties()]]
5. 配置类 写配置即可，不用想那些优化
6. 往前端返回的的数据，不用这么多,加保密原则，返回必要的即可。所以不用返回User,直接返回UserDTO,<mark style="background: #FFF3A3A6;">存储粒度优化</mark>
7. ![[Pasted image 20250430111743.png]]
	使用redis替代session存储
	![[Pasted image 20250430113643.png]]
	Redis有效期验证，设置有效期为30Min,但显然要是保持没有访问状态30min才会删除Redis的缓存用户信息，因此<mark style="background: #FFF3A3A6;">每当用户进行操作</mark>的时候，<mark style="background: #FFF3A3A6;">都要去更新</mark>Redis内部的用户信息有效期为30min。这个时候就使用拦截器功能
	![[Pasted image 20250430114610.png]]
8. cookie和session傻子解释
	[[cookie和session傻子解释]]
9. 拦截器中使用redis进行查询的话，必须要使用stringRedisTemplate,但是拦截器又没有使用spring的组件注解
	解决方案：[[自动注入]]
10. redis做类的属性插入的时候，防止一个属性一个属性插入而多次访问数据库造成性能低下
	直接先转成map
	```java
	Map<String, Object> userMap = BeanUtil.beanToMap(userDTO, new HashMap<>(),  
        CopyOptions.create()  
                .setIgnoreNullValue(true)  
                .setFieldValueEditor((fieldName, fieldValue) -> fieldValue.toString()));
	```
然后,直接putAll
```java
	stringRedisTemplate.opsForHash().putAll(tokenKey, userMap);
	```
11. ![[Pasted image 20250501155533.png]]
	优化原因，因为有很多<mark style="background: #FFF3A3A6;">其他的访问路径没有添加到这个拦截器里面</mark>来，比如商品页面访问，点评查看。但是又<mark style="background: #FFF3A3A6;">需要实现用户操作就更新redis清除数据时间的功能</mark>，所以配置两个拦截器。
	一个拦截一切途径，一个拦截需要登录的途径
![[Pasted image 20250501155914.png]]
<mark style="background: #FFF3A3A6;">设置拦截器优先级，registry.add后面加个.order(),里面值越小优先级越高</mark>
12. ![[Pasted image 20250501161745.png]]
13. ![[Pasted image 20250501162058.png]]
![[Pasted image 20250501162326.png]]
要使用缓存，就必须注意数据一致性，数据库中的数据更新，那么缓存中的数据也必须进行同步更新，否则可能会造成很大损失。
14. 如何添加缓存
![[Pasted image 20250502080716.png]]
15. 数据一致性
	![[Pasted image 20250502152109.png]]
	![[Pasted image 20250502152346.png]]
	3. 假如缓存中数据已经进行了很多次更新，但是异步线程还没有进行启动，那么此时缓存和数据库中的数据显然不一样。
		缓存是存储在内存中的，假如忽然断电，那么数据就会全部消失。
	<mark style="background: #FFF3A3A6;">一般选择方案1实现</mark>
16. ![[Pasted image 20250502153145.png]]
	方案三的操作注意的点
	![[Pasted image 20250502153709.png]]
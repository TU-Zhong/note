1. JsonUtil.toBean(String,target.class)
	将<mark style="background: #FFF3A3A6;">json字符串转为bean</mark>
	
2. JsonUtil.toJsonStr(target)
	JSONUtil.toJsonStr(List) <mark style="background: #FFF3A3A6;">将List</mark>转为json字符串
		<mark style="background: #FFF3A3A6;">将target类转为json字符串</mark>
	
3. JSONUtil.toList(shopTypeJson, ShopType.class);
	将字符串转为target类的list
	
4. 
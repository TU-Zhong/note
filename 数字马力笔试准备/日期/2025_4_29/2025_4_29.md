1. HashMap确实实现了Map接口,并且<mark style="background: #FFF3A3A6;">允许使用任何类型的对象作为键值对</mark>,同时也<mark style="background: #FFF3A3A6;">支持null</mark>作为<mark style="background: #FFF3A3A6;">键或值</mark>。这体现了HashMap的灵活性。
2. Math.floor()方法用于向下取整,即返回小于或等于参数的最大整数,返回值类型为double。
	不可强行转换
3. ArrayList的初始容量设置为20,这个容量大于默认的初始容量10且足以容纳初始数据,因此不需要进行扩容操作,所以扩充次数为0。
	[[Arraylist扩容]]
4. 方法重写和重载
	[[方法重写，方法重载]]
5. 变量名可以由字母、数字、下划线和美元符号组成,但必须以字母、下划线或美元符号开头。
6. volatile
	[[volatile使用]]
7. 父类和子类的加载顺序
	[[父类和子类的加载顺序]]
8. yield和sleep
	[[yield和sleep]]
9. idea 里面切换jdk版本，只需要在project structure中
	Project,Modules,SDKS以及settings里面的java compilers全部切换成同一版本即可完成切换

返回值
与data,methods的关系

响应式
ref创建数据（基本，对象）
proxy

从对象中解构出的值不是响应式的，要给对象添加toRefs关键字才能变成响应式的
```typescript
let {name,age} = toRefs(person)
```
toRefs,toRef

ref 修改一定要添加.value

双向绑定

表达式使用计算属性 computed 有缓存 只读，不可改
如果要改就把computed里面变成对象，实现get()和set()方法

<mark style="background: #FFF3A3A6;">标签 ref属性</mark>

<mark style="background: #FFF3A3A6;">reactive的用法，传入泛型</mark>
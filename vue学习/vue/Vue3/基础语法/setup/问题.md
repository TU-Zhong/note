返回值
与data,methods的关系

响应式
ref创建数据（基本，对象）
proxy

从对象中解构出的值不是响应式的，要给对象添加toRefs关键字才能变成响应式的
```typescript
let {name,age} = toRefs(person)
```
toRefs,toRef

ref 修改一定要添加.value

双向绑定

表达式使用计算属性 computed 有缓存 只读，不可改
如果要改就把computed里面变成对象，实现get()和set()方法

watch 监视数据变化
监视 ref,reactive ,函数返回的值，包含上述数据的数组
<mark style="background: #FFF3A3A6;">深度监视</mark> deep[[watch里面的深度监视是什么意思？]]
```typescript
const stopWatch=watch(sum,(newSum,oldValue)=>{  
  console.log('sum变化了', newSum, oldValue);  
  if(newSum>=10){  
    stopWatch();  
  }  
})
```
若修改的是ref定义的对象中的属性，newValue和oldValue都是新值，因为他们是同一个对象
若修改的整个ref定义的对象，newValue是新值，oldVaue是旧值，不是同一个对象了

而reactive包裹的全部都是修改的对象中的值，都是同一个对象
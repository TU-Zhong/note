好的，我们现在就来一步步实现消息发送功能。

我将按照我们之前讨论的四个步骤，为您提供每个步骤需要修改的具体代码和测试建议。请您在完成每一步后都进行相应的测试，这样可以确保问题被及时发现和解决。

---

### 第 1 步：协议层 - 创建消息数据包

**目标**：在 `protocol_parser` 模块中，增加一个新函数 `build_sendmsg_packet`，用于生成符合IPMSG协议的聊天消息数据包。

#### **实现**

1. **在 `protocol_parser.h` 中添加函数声明**： 打开 `src/core/protocol_parser.h`，在文件末尾（`#endif`之前）添加以下行：
    
    C
    
    ```c
    int build_sendmsg_packet(char* buffer, size_t buffer_size, uint64_t packet_no, const char* user_name, const char* host_name, const char* message);
    ```
    
2. **在 `protocol_parser.c` 中添加函数定义**： 打开 `src/core/protocol_parser.c`，在文件末尾添加以下函数的完整实现：
    
    C
    
    ```c
    int build_sendmsg_packet(char* buffer, size_t buffer_size, uint64_t packet_no, const char* user_name, const char* host_name, const char* message) {
        if (buffer == NULL || user_name == NULL || host_name == NULL || message == NULL) {
            return -1;
        }
    
        // 格式化数据包： 版本:包编号:用户名:主机名:命令:附加消息
        int len = snprintf(buffer, buffer_size, "%d:%llu:%s:%s:%u:%s",
            IPMSG_PROTOCOL_VERSION,
            packet_no,
            user_name,
            host_name,
            IPMSG_SENDMSG, // 使用发送消息的命令
            message);
    
        if (len < 0 || (size_t)len >= buffer_size) {
            return -2; // 缓冲区太小或发生错误
        }
    
        return len; // 返回数据包的实际长度
    }
    ```
    

#### **测试建议**

这一步是底层功能，单独测试不方便。我们可以暂时相信它的正确性，在完成第二步后，它将被网络层调用，届时我们可以一并测试。

---

### 第 2 步：网络层 - 收发聊天消息

**目标**：在 `communicator` 模块中，增加处理接收消息的逻辑，并创建一个新的发送消息函数。

#### **实现**

1. **处理接收消息**： 打开 `src/core/communicator.c`，找到 `process_packet` 函数中的 `switch` 语句，添加一个新的 `case` 来处理 `IPMSG_SENDMSG` 命令。
    
    C
    
    ```c
    // 在 process_packet 函数内
    switch (msg->command) {
        case IPMSG_BR_ENTRY:
            // ... (已有代码)
            break;
        case IPMSG_ANSENTRY:
            // ... (已有代码)
            break;
        case IPMSG_BR_EXIT:
            // ... (已有代码)
            break;
    
        // --- 添加以下 case ---
        case IPMSG_SENDMSG: {
            char message_buffer[MAX_PACKET_LEN];
            snprintf(message_buffer, sizeof(message_buffer), "[%s]: %s", msg->sender_name, msg->additional_section);
            // 调用已有的线程安全的打印函数
            console_print_message(comm->console, message_buffer);
            break;
        }
        // --- 添加结束 ---
    }
    ```
    
2. **创建发送函数**： a. 在 `src/core/communicator.h` 中，添加新的函数声明：
    
    C
    
    ```c
    void communicator_send_message(communicator_t* comm, const char* dest_ip, uint16_t dest_port, const char* message);
    ```
    
    b. 在 `src/core/communicator.c` 文件末尾，添加该函数的完整实现：
    
    C
    
    ```c
    void communicator_send_message(communicator_t* comm, const char* dest_ip, uint16_t dest_port, const char* message) {
        if (!comm || !dest_ip || !message) return;
    
        char buffer[MAX_PACKET_LEN] = {0};
        // 调用第1步创建的函数来构建数据包
        int packet_len = build_sendmsg_packet(buffer, sizeof(buffer), comm->packet_counter++, comm->user_name, comm->host_name, message);
    
        if (packet_len > 0) {
            struct sockaddr_in dest_addr;
            memset(&dest_addr, 0, sizeof(dest_addr));
            dest_addr.sin_family = AF_INET;
            dest_addr.sin_port = htons(dest_port);
            inet_pton(AF_INET, dest_ip, &dest_addr.sin_addr);
    
            // 发送数据
            sendto(comm->sock, buffer, packet_len, 0, (struct sockaddr*)&dest_addr, sizeof(dest_addr));
        }
    }
    ```
    

#### **测试建议**

此时，核心的收发逻辑已经完成。您可以暂时在 `main.c` 中硬编码一次发送来进行测试，但这比较麻烦。更建议您在完成第三、四步后，进行一个完整的端到端测试。

---

### 第 3 步：界面层 - 添加 `send` 命令

**目标**：修改 `console` 模块，使其能够识别并解析用户输入的 `send <用户编号> <消息>` 命令。

#### **实现**

1. **在 `console.h` 中更新结构体和函数声明**： a. 打开 `src/ui/console.h`，在 `console_t` 结构体中添加一个新的回调函数指针 `send_msg_cb`。
    
    C
    
    ```c
    typedef struct Console {
        // ... (已有成员)
        command_callback_t list_users_cb;
        command_callback_t send_msg_cb; // <-- 添加此行
        command_callback_t exit_app_cb;
        // ... (已有成员)
    } console_t;
    ```
    
    b. 修改 `console_register_callbacks` 的函数声明，增加一个新的参数来注册 `send_msg_cb`。
    
    C
    
    ```c
    void console_register_callbacks(console_t* console, void* context, command_callback_t list_cb, command_callback_t send_cb, command_callback_t exit_cb);
    ```
    
2. **在 `console.c` 中更新函数实现**： a. 修改 `console_register_callbacks` 的定义以匹配新的声明：
    
    C
    
    ```c
    void console_register_callbacks(console_t* console, void* context, command_callback_t list_cb, command_callback_t send_cb, command_callback_t exit_cb) {
        if (!console) return;
        console->callback_context = context;
        console->list_users_cb = list_cb;
        console->send_msg_cb = send_cb; // <-- 添加此行
        console->exit_app_cb = exit_cb;
    }
    ```
    
    b. 修改 `console_run` 函数，增加对 `send` 命令的解析逻辑：
    
    C
    
    ```c
    void console_run(console_t* console) {
        char buffer[256];
        while (!console->should_exit) {
            printf("> ");
            fflush(stdout);
    
            if (fgets(buffer, sizeof(buffer), stdin) != NULL) {
                buffer[strcspn(buffer, "\r\n")] = 0;
    
                if (strncmp(buffer, "send ", 5) == 0) { // 判断是否以 "send " 开头
                    if (console->send_msg_cb) {
                        char* args[2]; // 0: index, 1: message
                        char* context_ptr = NULL;
                        char* token = strtok_s(buffer + 5, " ", &context_ptr); // 跳过 "send "
                        if (token != NULL) {
                            args[0] = token; // 用户编号
                            args[1] = context_ptr; // 剩余的都是消息
                            if (args[1] != NULL) {
                                // 调用回调，传递2个参数
                                console->send_msg_cb(console->callback_context, 2, args);
                            } else {
                                console_print_message(console, "Usage: send <user_index> <message>");
                            }
                        } else {
                            console_print_message(console, "Usage: send <user_index> <message>");
                        }
                    }
                } else if (strcmp(buffer, "list") == 0) { // 注意这里要改成 else if
                    if (console->list_users_cb) {
                        console->list_users_cb(console->callback_context, 0, NULL);
                    }
                } else if (strcmp(buffer, "exit") == 0) {
                    if (console->exit_app_cb) {
                        console->exit_app_cb(console->callback_context, 0, NULL);
                    }
                } else {
                    if (strlen(buffer) > 0) {
                        console_print_message(console, "Unknown command.");
                    }
                }
            } else {
                // ... (已有代码)
            }
        }
    }
    ```
    

#### **测试建议**

现在还无法完整测试，但您可以临时在 `send_msg_cb` 被调用的地方加一句 `printf("Index: %s, Message: %s\n", args[0], args[1]);` 来验证命令解析是否正确。

---
[[完整main.c实现，这个可以运行和测试通过]]
### 第 4 步：逻辑层 - 连接UI与网络

**目标**：在 `main.c` 中实现 `send` 命令的回调函数，将用户输入和网络发送功能连接起来。

#### **实现**

1. **在 `main.c` 中创建新的回调函数**： 在 `main` 函数的上方，添加 `on_send_command` 函数的实现。
    
    C
    
    ```c
    // 在 on_list_command 和 on_exit_command 旁边
    void on_send_command(void* context, int argc, char** argv) {
        if (argc != 2) return; // 参数数量不正确
    
        user_list_t* user_list = (user_list_t*)context;
        communicator_t* comm = NULL; // 我们需要communicator的指针
    
        // !!注意: 这是一个简化处理，实际项目中应设计更优雅的方式来获取comm指针。
        // 这里我们假设可以从某个全局或共享结构中获取。
        // 为了让示例能跑起来，我们需要修改main函数，将comm也传给回调。
    
        // 暂时无法获取 comm 指针，我们先修改 main 函数
        // 等main函数修改后，再回头完成这里的逻辑
    }
    ```
    
2. **改造 `main` 函数以传递更多上下文**： `on_send_command` 函数需要 `user_list` 和 `communicator` 两个模块的指针。我们可以创建一个结构体来打包它们，作为统一的上下文。 a. 在 `main.c` 顶部，`main` 函数之前，定义一个上下文结构体：
    
    C
    
    ```c
    typedef struct {
        user_list_t* user_list;
        communicator_t* comm;
        console_t* console;
    } app_context_t;
    ```
    
    b. 现在重写 `on_send_command` 和 `on_list_command` 回调函数，使用这个新的上下文结构体：
    
    C
    
    ```c
    // 新的 on_list_command
    void on_list_command(void* context, int argc, char** argv) {
        app_context_t* app_ctx = (app_context_t*)context;
        console_display_users(app_ctx->console, app_ctx->user_list);
    }
    
    // 新的 on_send_command
    void on_send_command(void* context, int argc, char** argv) {
        if (argc != 2) return;
    
        app_context_t* app_ctx = (app_context_t*)context;
        int user_index = atoi(argv[0]); // 将字符串索引转为整数
        const char* message = argv[1];
    
        user_node_t users_snapshot[128];
        int count = user_list_get_all(app_ctx->user_list, users_snapshot, 128);
    
        if (user_index < 0 || user_index >= count) {
            console_print_message(app_ctx->console, "Error: Invalid user index.");
            return;
        }
    
        // 获取目标用户信息
        user_node_t* target_user = &users_snapshot[user_index];
    
        // 调用网络层发送消息
        communicator_send_message(app_ctx->comm, target_user->ipAddress, target_user->port, message);
    }
    ```
    
    c. 最后，修改 `main` 函数，使用这个新的 `app_context_t`：
    
    C
    
    ```c
    int main(int argc, char* argv[]) {
        // ... (前面的初始化代码不变) ...
    
        // 2. 初始化各个模块
        communicator_t comm = {0};
        user_list_t user_list = {0};
        console_t console = {0};
    
        // 创建上下文实例
        app_context_t app_ctx = { &user_list, &comm, &console };
    
        user_list_init(&user_list);
        console_init(&console);
    
        // 注册回调时，传入 app_ctx 的地址
        console_register_callbacks(&console, &app_ctx, on_list_command, on_send_command, on_exit_command);
    
        if (communicator_init(&comm, &user_list, &console, user_name, port) != 0) {
            // ... (错误处理)
        }
    
        // ... (后面的代码不变) ...
    }
    ```
    
    d. 别忘了修改 `console.h` 中的 `command_callback_t` 的 `send_cb` 参数，以匹配新的注册函数。
    

#### **最终测试**

现在所有代码都已完成！请重新编译整个项目。

1. 打开两个命令行窗口。
2. 在窗口A运行: `.\ipmsg_client.exe UserA 2425`
3. 在窗口B运行: `.\ipmsg_client.exe UserB 2426`
4. 在窗口A中输入 `list`，您会看到 UserB 的编号是 `[0]`。
5. 在窗口A中输入: `send 0 你好啊，B！`
6. 此时，您应该能在窗口B中看到打印出的消息：`[UserA]: 你好啊，B！`。

恭喜！您已经成功实现了完整的消息发送功能！